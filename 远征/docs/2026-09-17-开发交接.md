# 《远征》开发交接（2026-09-17）

> 本文面向**接手开发的新 Agent / 新会话**。目标是：读完这一篇就能直接跑起来、验证、继续开发，且不会重复踩已经踩过的坑。
> 单一事实来源仍是 [2026-09-15-玩法设计与开发路径.md](2026-09-15-玩法设计与开发路径.md)（v1.6，玩法规格）与 [开发规范.md](开发规范.md)（编码约定）；本文只做「现状 + 怎么跑 + 别踩什么」的交接。

---

## 1. 一句话现状

《远征》单机闭环已**全部打通并验证通过**：登录 → 创角/读档 → 主城（含召唤/兑换/设置）→ 出征筹备（含扫荡）→ 路线图 → 探索地图（遇敌/宝箱/篝火/商店/事件）→ 战斗（连携/换宠/药剂/撤退）→ 词条三选一 → BOSS 通关结算 → 回主城养成。

素材侧：`image/generated_*` 三批共 **286 张**（001_100 为 53 张、101_200 为 100 张、201_333 为 133 张），其中后两批各带一份 `ready/` 成品。`G.res_tex()` 已能寻址全部素材，**游戏内实际引用的 59 项经 `VerifyAssets` 验证 100% 解析成功**。

**9 个 headless 验证场景全绿**（新增 `VerifyGrowth`）；14 张界面截图已逐张人工复查通过（title / login / home / deploy / route / map / map_boss / battle / picker / city / city_built / gacha / exchange / settings），另补 `picker_school` 复核流派图标。

**养成 6 线已全量落地并验证**：天赋树（三系×10节点，每5级1点）/ 装备（强化+精炼+宝石，武器4系绑人物）/ 宠物（升级+突破+资质星级）/ 技能书（5技能×10级，每级k+5%）/ 坐骑（6类×2阶）/ 称号成就（12枚）。入口在主城「养 成」按钮（GrowthPanel），战斗侧经 `ally_cfg` 注入 `growth_bonuses` / `skill_levels` / `pet_stats` 生效。新增截图场景 `growth / talent / equip / pet_raise / skillbook / mount / titles` 已逐张复查通过。

### 1.1 画面整改批次（2026-09-17 晚，全部验证通过）

按用户截图反馈做的一轮「去 AI 廉价感」整改，9 项回归全绿，route/gacha/city/equip 截图复查通过：

1. **图鉴逐步解锁**：未收集卡显示「？？？」+ 暗色剪影 + 解锁方向提示；全解锁只在 GM 控制台（`G.gm_unlock_all_pets`）。
2. **ⓘ 详情弹层通用工厂**：`G.info_button(title, lines)` / `G.show_info_popup(anchor, title, lines)`（G.gd 末尾）。召唤出率保底、技能书规则、装备玩法、称号说明等长文案全部折叠进小圆圈按钮，面板不再堆字。技能行的 `k→` 术语改为玩家话术（「强度 +N％」「伤害大增」）。
3. **修复**：TraitPicker 词条说明截断（改 13px + 任意断行）；DeployPanel 宠物卡改上下结构（头像+稀有度线+单行名）；字体「回」字黑块根治（fontTools 删 cmap U+56DE + `font_serif.fallbacks=[font_bold]`；**改完必须删 `.godot/imported/ZCOOLXiaoWei*` 再 `--import`**，否则吃旧缓存）。
4. **引入 Kenney CC0 开源素材**（`image/generated_201_333/ready/ui_kenney/`，含 LICENSE.txt）：全局金剑鼠标指针（`G._apply_mouse_cursor`）、ⓘ 按钮木质圆底（`round_brown`）、路线图 HP 条三段式贴图（`ui_kenney_hp_back/fill`，由 3 段图 PIL 拼成，脚本 `tools/_stitch_bar.py`）。
5. **主城对话立绘位**：对话框左侧 76×76 木框半身像；寻址 `npc_<id>_portrait`，暂无新图时走别名映射（石头→npc_blacksmith、云游→npc_merchant、阿豆→npc_courier），旅人暂无。
6. **NPC 素材需求文档已交付用户**：`docs/人物素材需求.md`——7 位 NPC + 旅人的外观设定、可复制提示词、idle 512×128 / walk_4dir 512×512 / 立绘 512×512 规格、命名与放置目录、交付检查清单。**等用户生成完放入 ready/npcs/ 后，接 `CityScene._CityNPC` 贴图化即可**（接线步骤已写在文档 §5）。

### 1.2 滑动分页 + 加载页批次（2026-09-17 深夜，全部验证通过）

按用户反馈「页面拥挤改滑动 + 旧背景回归加载页」做的一轮，9 项回归全绿，load/deploy/worlds/codex 截图复查通过：

1. **通用分页组件 `src/ui/PageDeck.gd`**：水平滑动分页容器——手指/鼠标拖拽翻页（松手按位移判定 + 边界阻尼回弹）、左右箭头（Kenney arrowBrown）、键盘 ←→/↑↓/WASD 全支持、底部圆点指示器；单页时箭头圆点自动隐藏。三个面板共用，**新面板要分页直接 preload 它**。注意：颜色常量不可引用 autoload G（headless 加载期报错），已写死色值。
2. （已由 §1.3 取代）**DeployPanel 三页化**：秘境（2×4 大卡）/ 人物（2×2 大卡 196×150，立绘放大）/ 宠物（2×4 大卡）各占一页，底部按钮区不变。
3. （已由 §1.3 取代）**WorldPanel 两页化**：每页 4 片大陆（行高 48→56、间距 58→70）。
4. （已由 §1.3 取代）**CodexPanel 两页化**：每页 4 张卡 2×2（卡 104→128 高）。
5. **加载页 `src/ui/LoadScreen.gd/.tscn`**：Main 场景流改为 Load → Title。背景回归旧星空秘境 `enter.png`（用户点名找回的那张）+「远征」标题 + Kenney 三段式进度条；预热 = 素材索引全量 + 四人行走帧，分帧每帧 9 张不卡帧；**最短显示 1s**（不足时 tween 补足再切场景）。加载文案轮换（整备行囊→点起篝火→擦拭兵器→查看星图→启程）。
6. **坑 9（新增）**：GDScript `%` 格式化的数组跨行写法 `"%s" % [a,\n b]` 会报 `Closing ] doesn't match opening "["`——把下标表达式先算进变量再格式化，且字面百分号用全角％或写进变量。

### 1.3 一屏一项大卡轮播批次（2026-09-17 深夜，全部验证通过）

`SlideCard.gd` + `PageDeck.gd`（升级）把选人/宠物/世界/图鉴改成滑动大卡，全部验证通过。

**面板改动**：WorldPanel / CodexPanel 各 8 页大卡；DeployPanel 改页签（秘境/人物/宠物）+ 每页大卡，`← →`/AD 翻选项、`↑ ↓`/WS 换页签；PetRaisePanel 顶部换成灵宠大卡；CreateRole 支持左右拖动切职业 + 预留职业插画位。

**待补插画**：秘境 8 张 `world_<theme>.png`；缺图时卡上印出文件名，生成后丢进 `image/generated_*/ready/` 即生效。

**坑 10**：`art_ratio` 只当插画高**下限**（余量由 art 的 EXPAND + stretch_ratio=8 吸收），别当实际高；卡里塞多行散文会超页高被裁。

**组件口径**：`PageDeck` 升级为一屏一项——`key_mode` 选吃哪组键（both/lr/ud/none）、`wrap` 循环、圆点画在 clip 外的页脚带；`SlideCard.page(card, w, h)` 负责把卡居中并留出箭头两侧 gutter。

---

### 1.4 音频与设置补全批次（2026-09-17，全部验证通过）

1. **`src/autoload/Audio.gd`（新 autoload）**：`Audio.play_bgm("bgm_home")` / `Audio.sfx("ui_click")`。
   按文件名在 `assets/audio/` 找 `.ogg/.wav/.mp3`，**缺文件静默降级**（先跑框架，素材后补）；
   换曲带淡入淡出；音量/静音存进存档 `G.audio`（拖动时 0.5s 防抖落盘）。
2. **已接线**：GameHome/Title/CityScene/RouteScene/MapScene/BattleScene 各起一首 BGM；
   `G.gold_button` 与 `G.info_button` 已挂 `ui_click` 点击音（全局按钮都有）。
3. **设置面板**：新增「声音」区（音乐/音效滑条 + 静音开关，金色滑条皮肤手工绘制）；
   键位说明改收进右上角「?」；面板仍是 440×524，返回键下移到 462。
4. **待补素材**：`assets/audio/README.md` 列了文件名与 CC0 来源（Kenney / OpenGameArt / FreePD / Pixabay），
   丢进目录即生效，无需改代码。
   **（2026-09-18 轮次 5 已闭环）**：18 件 UI/奖励/战斗音效改由 `tools/make_sfx.py` **程序合成**（自产、可重生成）；
   `Audio.gd` 升级为 **8 播放器音效池 + ±3% 随机音高**；全流程接线（按钮/翻页/浮层/战斗/奖励/委托）完成，回归见 `VerifyAudio`。

### 1.5 主城排版改版（2026-09-17，全部验证通过）

按用户给的参考（手游主页那种排版）重排 `GameHome`，风格不变（羊皮纸/金/宋体）：

1. **顶部玩家条**：名字 + LV + 经验条 + 秘境进度合成一条 432×56 的深底金边横条（替代原来的底部羊皮纸信息卡，原 `_build_info` 已删）。
2. **左右功能列**：左列「主城/世界/图鉴」、右列「养成/兑换/召唤」，圆钮（60 圆底 + 单字纹样 + 下方小字），位置 232/324/416；原来平铺在钱包下的三个金按钮已移除。
3. **底部主操作**：「出征」放大成 300×64 的主按钮（全屏最醒目）；左下角留圆形「设置」。
4. **立绘放大**：`SPRITE_SCALE` 1.9 → 2.2，圆台 `PED_Y` 604，主界面以角色为视觉中心。
5. **创建角色页**：职业插画压成氛围层（alpha 0.34、边框 0.16、框缩到 336×246）—— 插画与台上像素小人是同一角色，之前两张"两个破军"打架。

## 2. 环境与跑法

| 项 | 值 |
|---|---|
| 引擎 | Godot 4.6.1 stable（`D:\godot\Godot_v4.6.1-stable_win64.exe`） |
| 项目根 | `d:\godot\new bee\远征` |
| 主场景 | `res://src/main/Main.tscn` |
| 视口 | 480×800 竖屏，Compatibility 渲染 |
| 终端 | **PowerShell**（不是 bash，见 §7 坑 5） |

> **2026-09-18 起**：连续自动迭代的每轮记录都在 [2026-09-18-夜间迭代日志.md](2026-09-18-夜间迭代日志.md)（已到轮次 7）；素材许可在 [素材来源与许可证.md](素材来源与许可证.md)。回归清单已扩至 14 个场景（`VerifyPerf` 耗时预算 + 按钮命中、`VerifyLore` 世界观与演出、`VerifyQuests` 每日委托、`VerifyAudio` 音效素材/音效池、`VerifyTransit` 转场与硬切防回退）+ 5 个 `-s` 脚本模式用例（共 19 项）。**切场景一律走 `G.go(path)`**（统一转场 + 防连点），别直接 `change_scene_to_file`——`VerifyTransit` 会扫描源码抓硬切（白名单只有 G 自身与战斗兜底）。

### 2.1 验证（headless，改完代码必跑）

```powershell
$godot = "D:\godot\Godot_v4.6.1-stable_win64.exe"
$proj  = "d:\godot\new bee\远征"
$scenes = @("VerifyAssets","VerifyBattleScene","VerifyCity","VerifyGameHome",
            "VerifyMapScene","VerifyRouteScene","VerifyGacha","VerifyPanels","VerifyGrowth",
            "VerifyPerf","VerifyLore","VerifyQuests","VerifyAudio","VerifyTransit")
foreach ($s in $scenes) {
  $out = (& $godot --headless --path $proj "res://tools/$s.tscn" 2>&1 | Out-String)
  $ok  = ($out -match "_OK all tests passed") -or ($out -match "ASSETS_OK")
  $bad = ($out -match "FAIL:") -or ($out -match "ASSETS_FAIL")
  "$s : " + $(if ($ok -and -not $bad) { "PASS" } else { "FAIL" })
}
```

判定口径：输出出现 `*_OK all tests passed` / `ASSETS_OK` 即通过；出现 `FAIL:` 则失败。

> `VerifyPanels` 会打印一行 `ERROR: Parse JSON failed`——这是「非法存档码」用例的**预期报错**，不是失败，只要结尾是 `PANELS_OK` 即可。

### 2.2 截图（窗口模式，非 headless）

```powershell
& $godot --path $proj res://tools/ShotRunner.tscn -- --scene=gacha --frames=60
```

产物：`C:\Users\tsz\AppData\Roaming\Godot\app_userdata\远征\shots\<scene>.png`

可用场景名见 [ShotRunner.gd](../tools/ShotRunner.gd) 的 `match _scene`（**load** / title / login / createrole / home / deploy / worlds / codex / gm / gm_open / route / map / map_boss / battle / picker / picker_school / city / city_built / city_deploy / quests / **story_intro / story_outro** / gacha / exchange / settings / **growth / talent / equip / pet_raise / skillbook / mount / titles**——养成线 7 个为演示档；`load` 截图要用 `--frames=30`，60 帧整好赶上它 1s 切场景会截到空灰帧）。

---

## 3. 项目结构速查

```
远征/
├─ data/            # 全 JSON 表驱动（改数值只动这里，别写进代码）
│   roles / skills / traits / combos / pets / growth
│   maps / monsters / nodes         # 世界·怪物·节点
│   city / exchange / gacha         # 主城建筑 / 荣誉兑换（14条，含养成材料）/ 召唤奖池
│   talents / equip / skillbook / mounts / titles   # 养成 6 线数值表
├─ src/
│   main/Main.gd            # 场景流：Title → Login → 有档主城 / 无档创角
│   autoload/G.gd           # 全局：存档、钱包、素材索引 res_tex、UI 工厂（gold_button/parchment_box/…）、场景转场 G.go
│   combat/                 # 纯逻辑：BattleSim / DamageCalc / SkillSystem / TraitSystem / MonsterAI / TableCache
│   battle/BattleScene.gd   # 战斗表现层
│   explore/MapScene.gd     # 探索大地图（遇敌、物件交互、传送阵）
│   run/RouteScene.gd       # 路线图 + 局结算
│   city/CityScene.gd       # 远征主城（建筑/NPC/据点码）
│   ui/                     # 所有浮层：Title/Login/CreateRole/Prologue/GameHome/DeployPanel/QuestPanel/
│                           #   WorldPanel/CodexPanel/TraitPicker/GachaPanel/ExchangePanel/SettingsPanel/GmConsole
│                           #   StoryBeat（首领前对峙/战后余韵演出）
│                           #   养成线：GrowthPanel（6线主页）+ Talent/Equip/PetRaise/SkillBook/Mount/Title 6 子面板
├─ tools/                   # 验证与截图工具 + 素材预处理 Python 脚本
└─ image/
    generated_001_100/source/     # 53 张（无 ready）
    generated_101_200/{source,ready}/   # 各 100 张
    generated_201_333/{source,ready}/   # 各 133 张
    background/ map/ map2/ map_proc/ role/ role2/   # 早期手工整理素材
```

Autoload：`G`（`src/autoload/G.gd`）、`GmConsole`（`src/ui/GmConsole.gd`）。

> `image/generated_001_050/source/` 是**空目录**（0 张 png），历史遗留，索引未扫它也无影响。

---

## 4. 素材取用机制（本轮刚修，务必理解）

```gdscript
G.res_tex("ui_banner_gacha")   # 按名取纹理，缺失返回 null（调用方自备回退画法）
G.res_path("...")              # 同上，返回路径
```

`G._build_res_index()` 的解析顺序：

1. **先扫 `ready/`**（递归各子目录，文件名即 key，如 `ui_banner_gacha.png`）
2. **再扫 `source/` 补位**（剥掉 `NNN_` 编号前缀作为 key）

**为什么必须 ready 优先**：`source/` 是 970~2170px 的原始 AI 母图，`ready/` 才是裁到设计尺寸、已硬化 alpha 的成品。之前只扫 `source/` 导致每个 UI 图标都在加载千像素大图（显存浪费），且会把 TextureRect 撑爆。

---

## 5. 必须遵守的硬性约束

1. **登录流程不可省**：`Title →「开始游戏」→ Login → 有档进主城 / 无档创角`。登录界面不能被存档跳过。
2. **反 AI 模板审美**：柔和下投影（`G._apply_shadow`）；圆角**刻意微差**（5/7/6/4 这类，别做成规整矩形）；金色按钮要有按压反馈（微暗 + 微缩回弹）。
3. **解锁 与 通关 是两种状态**，UI 上必须能区分。
4. **窗口居中、可关闭**。
5. **UI 一律走 `G.gd` 的主题工厂**（暖棕 + 羊皮纸 + 金；字号阶梯 13/16/18/22/30/56）；**文字严禁烧进图片**。
6. **数值全部表驱动**，不要硬编码进 GDScript。
7. 发现素材本身的问题，记进 [素材问题清单.md](素材问题清单.md)。

### 浮层布局约定（照着写就不会错位）

```gdscript
var panel := G.parchment_box(440, H, 16.0)   # 宽 440
panel.position = Vector2(20, 108)            # 固定左上角
var content := Control.new()                 # PanelContainer 是容器，必须包一层 Control 手动布局
content.set_anchors_preset(Control.PRESET_FULL_RECT)
panel.add_child(content)
# 子控件坐标基准 = 440 - 16*2 = 408
const CONTENT_W := 408.0
```

---

## 6. 本轮修掉的坑（**别再踩**）

### 坑 1｜`TextureRect` 的 `expand_mode` 必须在 `size` **之前**赋值

`Control.size` 只会被「顶大到不小于最小尺寸」，**不会自动缩回**。先设 `size` 再设 `expand_mode`，最小尺寸瞬间被钳到贴图原尺寸，`size` 随后被顶成原图大小。

症状：召唤横幅被撑成 **2172×724** 铺满面板，把「保底/出率/炼化规则」全压住不可读。

```gdscript
tr.expand_mode = TextureRect.EXPAND_IGNORE_SIZE   # ← 先这个
tr.custom_minimum_size = Vector2(w, h)
tr.size = Vector2(w, h)                           # ← 再这个
```

已修位置：`GachaPanel._tex_rect`、`TraitPicker` 流派图标、`CodexPanel` 头像与稀有度边框、`BattleScene` 战斗背景。**新写 TextureRect 时同样注意顺序。**

### 坑 2｜`PanelContainer` 会把每个直接子控件都铺满内容区

往 `G.gold_button()` 的返回值上再 `add_child` 一个 Label，两个 Label 会**完全重叠**（曾导致「十 连」盖在「800 魂石 / 1 券」上）。两行按钮必须用 `VBoxContainer` 竖排（见 `GachaPanel._gold_btn2`）。

**同款变体（养成线实证）**：自建 `PanelContainer` 行（root）+ 内部 Control（inner）结构时，按钮若误 `root.add_child(btn)` 会被铺满整行，遮住同行的名字/等级等文本（SkillBookPanel 技能行、TitlePanel 称号行均踩过）。**按钮一律加到 inner，且 inner 设 `mouse_filter = IGNORE`、按钮设 `STOP`。**

### 坑 7｜新 `class_name` 不进全局类缓存，headless 直接编译失败

`.godot/global_script_class_cache.cfg` 是编辑器侧维护的，**命令行/CI 环境下新写的 `class_name`（如 TalentPanel 等 7 个养成面板）不会自动登记**；其他脚本用类名引用或做类型标注（`var x: GrowthPanel`）会在 headless 下编译失败。
**做法：一律 `const FooScript := preload("res://src/ui/Foo.gd")` 后 `FooScript.new()`，变量标注用 `Control` 等基类**（项目既有惯例，见 `VerifyPanels.gd`）。

### 坑 8｜GDScript 字符串 `%` 格式化里不能出现字面量 `%` 紧跟非格式字符

`"...+6%，实为 %.3f"` 会抛 `unsupported format character`（`，` 被当格式符）。**字面量百分号改全角 `％`**，或写 `%%`。

### 坑 3｜GDScript lambda 捕获局部变量是**值拷贝**

```gdscript
var base_y := 0.0
root.ready.connect(func(): base_y = root.position.y)   # ✗ 外层永远看不到
```
曾导致 `TraitPicker` 悬停时 tween 到 `0-6=-6`，把整张卡推出屏幕。
**正确做法**：用 `set_meta` / 节点属性 / 数组容器传递可变状态。

### 坑 4｜Variant 推断警告被项目当错误

```gdscript
var mon_id := (lead_mon if i == 0 else pick.call())   # ✗ 编译失败（从 Variant 推断）
var mon_id: String = (lead_mon if i == 0 else pick.call())   # ✓ 显式标注
```
三元表达式 / `Dictionary.get()` 混类型时，一律**显式标注类型**。

### 坑 5｜终端是 PowerShell，不是 bash

- `tail` 不存在 → 用 `Select-Object -Last N`
- `Join-String` 在 PS 5.1 不存在 → 用 `-join` 或字符串拼接
- 给 Godot 传用户参数要加 `--` 分隔：`... ShotRunner.tscn -- --scene=gacha`
- 批量跑长时间命令时，`Start-Process -ArgumentList` 拼数组容易踩参数绑定错误，直接用 `& $exe args... | Out-String` 更稳

### 坑 6｜写完后台子代理产出必须主线程验证后再接线

不要直接信子代理的「已完成」，先跑对应 `VerifyX.tscn`。

### 已修的历史 bug（供追溯）

- `DeployPanel` 的 `var _role_btns := {}` 曾被误删 → 导致 VerifyCity/GameHome/BattleScene 全链编译失败。**改变量块时注意别误删临近声明。**
- `VerifyRouteScene` 结算校验未计入「毫发无损 +10%」加成 → 已加 `scene._bonus_gold`。

---

## 7. 未提交改动概览（`git status`）

历史各轮改动已分批提交（最近 6 次：`539658e docs: 设计文档升至v1.6…`、`9c2faf0 test(tools): 补齐验证与截图工具…`、`f0ac41c feat(gameplay): 宠物等级成长…`、`1ad5766 feat(core): 全局系统层`、`e4c3c9a feat(assets): 全量素材接线`、`f101796 feat(ui): 召唤/兑换/主城等浮层`）。

**当前工作区为「养成 6 线 + 画面优化」一轮的未提交改动，共 28 条**：

- **14 条未跟踪**：`data/{talents,equip,skillbook,mounts,titles}.json`、`src/ui/{GrowthPanel,TalentPanel,EquipPanel,PetRaisePanel,SkillBookPanel,MountPanel,TitlePanel}.gd`、`tools/VerifyGrowth.{gd,tscn}`
- **14 条修改**：`G.gd`（养成API+存档扩展）、`BattleSim.gd`（养成接入）、`TableCache.gd`（新表访问器）、`MapScene.gd`、`exchange.json`（扩至14条）、`maps.json`、`ShotRunner.gd`（养成演示档）、`VerifyPanels.gd`、`GameHome.gd`（养成入口）、`CodexPanel/DeployPanel/GachaPanel/TraitPicker`（画面优化）

**尚未提交。** 接手后建议先确认一次提交边界（用户此前未授权提交，故未执行）。

---

## 8. 已知遗留问题（不阻断运行）

1. `bg_battle_snowfield` 命名与其余 7 张 `bg_battle_<主题>` 不一致（主题 id 是 `snow`）。
   `maps.json` 的 `battle_bg` 已正确指向，功能无影响。**`VerifyAssets` 特意改成读 `battle_bg` 字段而非按 `bg_battle_{theme}` 拼名**，就是为了不踩这个坑——新增素材时注意命名一致。
2. 召唤面板活动横幅 `ui_banner_gacha` 是 3:1（480×160），压在 110px 展示框里会被 `STRETCH_KEEP_ASPECT_COVERED` 裁掉上下两端。当前裁切后主体纹样完整；若要完整展示，改 `KEEP_ASPECT_CENTERED` 或单独出一版专用比例。
3. `素材问题清单.md` 里另有几条「轻微」级未处理项（`100_pet_eyescat` 单/双尾设定待策划确认、`062/063/064` 晕染风与像素风混搭等），详见该文件。
4. GM 控制台口令与用法见 [玩法设计与开发路径.md](2026-09-15-玩法设计与开发路径.md) §14（F10 / `` ` `` 唤起）。

---

## 9. 建议的下一步

按价值排序：

1. **主城 NPC 贴图化**：等用户按 [人物素材需求.md](人物素材需求.md) 生成完 idle 图后，给 `CityScene._CityNPC` 加「贴图优先、色块回退」（接线步骤在该文档 §5）。
2. **补齐面板级截图复查**：`图鉴/设置/兑换/商店/世界图志/GM 控制台` 尚未逐张过目。
3. **回头核对 `ready/` 新索引下所有面板的取图尺寸**：`VerifyAssets` 只验证「能否解析」，不验证「尺寸是否合适」。
4. **PVP（§7）**：目前完全未实现，`data` 层与 `skills/combos` 已具备复用条件；设计文档 §7 已给出模式矩阵 / 匹配 / 锁步同步 / 结算段位的完整规格。
5. **打怪掉落表**：`pets.json` 的 `unlock.type = drop` 仍为 `[规划]`。

---

## 10. 交接自检清单（接手后请依次确认）

- [ ] 用 §2.1 脚本跑一遍，确认 9 项全 PASS
- [ ] 用 §2.2 截 3 张图（如 `home` / `battle` / `growth`），确认画面正常
- [ ] 通读 [玩法设计与开发路径.md](2026-09-15-玩法设计与开发路径.md) 的 §0（全局逻辑链）、§6.3（养成主线与解锁来源）、§13（历史修正）
- [ ] 通读 [开发规范.md](开发规范.md)
- [ ] 确认本轮未提交改动是否要先落一次 commit（需用户授权）
