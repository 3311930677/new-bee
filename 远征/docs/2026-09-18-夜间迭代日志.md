# 《远征》夜间自动迭代日志（2026-09-18 起）

> 用户指令：以「参考热门游戏、优化界面与玩法、减少 AI 感、丰富剧情背景」为总目标，连续做 10 轮自动迭代。
> 每轮固定动作：①查现状排优先级 → ②1 个主问题 + ≤5 个副问题 → ③动手改 → ④跑全部回归（13 项 headless + 截图）→ ⑤更新日志/素材来源/下轮计划 → ⑥git 提交。
> 运行环境：Godot 4.6.1 stable（`D:\godot\Godot_v4.6.1-stable_win64.exe`），项目根 `d:\godot\new bee\远征`，PowerShell。
> 回归命令见 [2026-09-17-开发交接.md](2026-09-17-开发交接.md) §2.1（本轮起多一项 `VerifyPerf`）。

---

## 轮次 1｜进界面慢 + 按钮点不动（性能与输入手感的硬 bug）

**用户原始反馈**：「现在进入游戏界面很慢是为什么，然后返回按钮和一部分按钮经常失灵，检查按钮」

### 1.1 诊断（先量，再改）

写了一次性探针（`tools/_ProbeUI.gd`，用完即删）分别量「首次 / 二次」进入各屏的耗时，结论全在数字里：

| 观测项 | 数值 | 说明 |
|---|---|---|
| 素材索引全量扫描（336 项） | 4 ms | 不是瓶颈 |
| 加载页预热 275 张 ready 贴图 | 381 ms | 一次性，可接受 |
| 单件大图 `background/home.png`(1.5MB) | 23 ms | 不是瓶颈 |
| 单件 `bgm_home.ogg`(1.4MB) | 4 ms | 不是瓶颈 |
| **GameHome 首次进场景** | **533 ms** | ← 问题在这 |
| GameHome 第二次进场景 | 31 ms | 差值 500ms 全是一次性开销 |
| CityScene 首次 / 二次 | 212 / 113 ms | 同上 |
| 打开世界图志 / 图鉴 / 召唤 / 兑换 / 设置 / 养成（首次） | 121 / 87 / 79 / 25 / 13 / 31 ms | 每个面板首次都要付一次 |

**决定性实验**：把面板脚本逐个 `load()` 计时，再进场景 —— `GrowthPanel.gd` 148ms、`GameHome.tscn` 231ms、`RouteScene.tscn` 236ms（连带 MapScene/BattleScene）、`GachaPanel.gd` 52ms、`WorldPanel.gd` 41ms…**全部编译完后再进 GameHome 只要 56ms**。
结论：慢的不是贴图、不是音频、不是字体——是 **GDScript 的首次编译**，而且它被平摊到「玩家点进每个界面的那一帧」上，所以感知成「进界面很慢」。

**按钮为什么失灵**：金剑鼠标指针贴图 34×37，刃尖在左上 `(0,1)`，而热点写成了 `(4,33)`（那一点在贴图的透明空白处）。热点定义是「贴图的哪一点压在鼠标坐标上」，于是**整把剑被画到鼠标上方 32px**——玩家拿剑尖去点按钮，真实落点在剑尖下方 32px，38px 高的按钮正好被点空。这不是控件被挡。

顺带用命中测试（复刻 Godot 的 `gui_find_control_at_pos`）逐点验证，另有真死区：主城「出征」按钮左上角被摇杆的 124×124 命中区咬掉 7×26px、右下角被提示条咬掉 71×20px（合计约 78×26 的角部死区）。

### 1.2 本轮改动

| # | 类别 | 改动 | 文件 |
|---|---|---|---|
| 主 | 输入 | 指针热点 `(4,33)` → `(1,1)`（压在剑尖上），并写清为什么 | `src/autoload/G.gd` |
| 副 1 | 加载 | **加载页接管一次性编译**：新增 `PRELOAD_CODE`（主界面/主城/路线图 3 个场景 + 6 个面板脚本 + 创角/标题/登录）与 `PRELOAD_AUDIO`（6 首 BGM），连同贴图分帧预热；进度条按「贴图 + 脚本」总数推进 | `src/ui/LoadScreen.gd` |
| 副 2 | 输入 | 主城装饰层不再抢点击：城名横幅、等级签、操作提示条、摇杆左移 12px | `src/city/CityScene.gd` |
| 副 3 | 缓存 | `G.res_tex()` 加名称级贴图缓存（面板反复开关少几十次 `load()` 往返） | `src/autoload/G.gd` |
| 副 4 | 性能 | 主城暗角渐变 480×800 逐像素生成 → 120×200 + 线性过滤 | `src/city/CityScene.gd` |
| 副 5 | 工程 | 新增性能/按钮回归工具 `tools/VerifyPerf.gd/.tscn`（带预算断言 + 五点命中测试）；加载页加测试接口 `auto_advance` | `tools/`、`src/ui/LoadScreen.gd` |

### 1.3 效果（同一台机器，headless 实测）

| 指标 | 改前 | 改后 |
|---|---|---|
| **GameHome 进场景** | 533 ms | **47 ms** |
| **CityScene 进场景** | 212 ms | **113 ms** |
| 世界图志首次打开 | 121 ms | 119 ms（脚本已预热；剩下的 8 张大卡结构成本留待后续轮次虚拟化） |
| 图鉴 / 召唤 / 兑换 / 设置 / 养成 | 87 / 79 / 25 / 13 / 31 ms | 86 / 16 / 32 / 14 / 17 ms |
| 二次打开任一浮层 | — | 14~18 ms |
| 主城「出征」按钮可利用区域 | 缺两个角 | 五点全中 |
| 六个浮层「返回」+ 主城「回营/出征」 | — | 九处 × 五点全中 |
| 加载页总时长（含编译） | ~1.0 s（只有贴图） | 1.18 s（把全流程一次性开销提前付掉，显示进度条与轮换文案） |

### 1.4 验证

- 13 项 headless 回归全绿：`VerifyAssets / VerifyBattleScene / VerifyCity / VerifyGameHome / VerifyMapScene / VerifyRouteScene / VerifyGacha / VerifyPanels / VerifyGrowth / VerifyPerf` + `verify_data / verify_battle / verify_route / verify_trait / verify_walk_assets`。
- 截图复查：`city / city_built / home` 三张人工过目——NPC 全身立绘、暗角平滑无带状、提示条不再压住主按钮。
- 回归守卫：`VerifyPerf` 把预算写死成断言（主界面 200ms、主城 250ms、浮层 130ms、二次打开 70ms），今后谁再把一次性开销放回「点进界面那一帧」就会红。

### 1.5 未解决 / 风险

- 世界图志首次打开仍要 ~119ms（8 张 SlideCard 一次性建完）；正解是 PageDeck 按需建卡（用到才建），排进后续轮次。
- 主城 113ms 里大头是 480 格 `TileMapLayer.set_cell` 与 8 个 NPC 节点，属结构成本，暂不优化。
- **音效目前是空的**：`assets/audio/ui_click.wav` 在本次会话的工作区里是「已删除」状态（未提交），所以 `Audio.sfx("ui_click")` 静默无输出，按钮无点击声。按用户此前意图不擅自恢复，改为后续轮次统一做一套克制的 UI 音效（CC0 或程序合成）。

### 1.6 下轮计划（轮次 2）

轮次 2 主问题：**核心玩法的「剧情背景」缺失**——现在从创角直接进主城，世界/秘境/首领都只有一行说明，玩家不知道自己为什么出征。
计划：加一段**可跳过的开场叙事**（世界背景 + 主角身份 + 首个目标），数据表驱动（`data/lore.json`），风格走羊皮纸书卷（宋体竖排感 + 逐行淡入），并在主界面加「当前主线目标」一行，让目标感贯穿。

---

## 轮次 2｜世界志与序章：把"你为什么出征"讲清楚

**现状缺口**（按影响大/成本低排序后选的主问题）：
1. **主问题｜剧情背景几乎为零**：创角完成后直接落进主城，玩家不知道「昭元界是什么」「碑为什么会碎」「第一站该去哪」。八片大陆只有名字和一句状态，首领除了名字什么都没有。
2. 副｜主线目标不可见：只有世界图志里的「已解锁 3/8」这类统计，没有"我现在该干什么"。
3. 副｜旧存档没有引导衔接：新加的叙事如果无脑弹，会打断老玩家。
4. 副｜文案散在代码里：`GameHome`/`WorldPanel` 里已有中文长句，再加剧情会越来越难改。
5. 副｜没有剧情回归手段：文案与配置脱节（比如首领改名）不会被任何测试发现。

### 2.1 本轮改动

| # | 类别 | 改动 | 文件 |
|---|---|---|---|
| 主 | 剧情 | 新增世界观数据表 `data/lore.json`：界名/纪年/局势/主角身份 + **5 页序章** + **八境志异**（题记 / 正史 / 此地故人 / 首领来历）+ 目标文案模板 | `data/lore.json`（新） |
| 主 | 剧情 | 新增**序章卷轴页** `Prologue`：首屏自动补一页「世界志」（界名+局势+你是谁），其后 5 页逐页翻；正文**段首空两格 + 逐行淡入**；点卷轴/点「继 续」/回车翻页，右上「跳 过」；看完或跳过都记为已看 | `src/ui/Prologue.gd/.tscn`（新） |
| 副 1 | 剧情 | 流程接线：`Login`（有档未看序章 → 序章）与 `CreateRole`（捏完人 → 序章）都先过序章再入城 | `Login.gd`、`CreateRole.gd` |
| 副 2 | 目标感 | 主界面木匾下方常驻一行「目标 · 大陆名 · 讨伐「首领」」，点开是完整世界志（题记/正史/故人/首领来历 + 下一步指引）；宽度收到 304 避开右侧竖列 | `GameHome.gd`、`G.gd`（`main_goal()` / `main_goal_short()`） |
| 副 3 | 剧情 | 世界图志与出征筹备的**秘境卡都加上题记**，选地方之前先读出这片地方的味道 | `WorldPanel.gd`、`DeployPanel.gd` |
| 副 4 | 工程 | 首领名**从 maps.json 的 boss id 去 monsters.json 取**（不在 lore.json 里再抄一份名字，避免两处不同步）；`TableCache.lore_config()` | `G.gd`、`TableCache.gd` |
| 副 5 | 回归 | 新增 `tools/VerifyLore.gd/.tscn`：八境志异完整性、首领名可解析、主线目标随进度推进（含"碑门推不开"的锁定分支与全通关收束）、`lore_seen` 存档往返、序章能逐页走完 | `tools/VerifyLore.*`（新） |
| — | 存档 | `prog.lore_seen` 写入读档（老档缺省 false） | `G.gd` |
| — | 加载 | 序章场景加入加载页预热清单 | `LoadScreen.gd` |

**叙事梗概（写进 lore.json，游戏内可读）**：昭元界八座镇界碑锁着地脉下的「渊」；天倾纪十一年林海那座碑先哑了——**不是被打破的，是从里面推开的**。你是守碑人最后一脉，师门散尽只剩半卷《远征图志》。八境各有碑灵化成的首领（森林之主是把自己种进树心的守碑人、雪原女巫是第一个听见碑里声音的人……），而终局的篡位者之王曾是守碑人首座——他算过一笔账：八碑归位，昭元要死一半人才能活。

### 2.2 验证

- 14 项 headless 回归全绿（新增 `VerifyLore`）：`VerifyLore / VerifyGameHome / VerifyCity / VerifyPanels / VerifyPerf / VerifyGacha / VerifyGrowth / VerifyAssets / VerifyMapScene / VerifyBattleScene / VerifyRouteScene` + `verify_data / verify_battle / verify_route / verify_trait / verify_walk_assets`。
- 截图复查：`prologue`（首屏世界志）、`home`（目标行）、`worlds`（题记+状态两行）、`deploy_plain`（新增截图用例，压掉引导弹层看卡片版式）——四张均正常，两行文案未被裁切。
- `VerifyPerf` 预算仍全绿（主界面加了一行目标文案后仍是 47ms 级）。

### 2.3 未解决 / 风险

- 序章目前**没有声音**（BGM 沿用 title 曲，且点击音效缺失），下一轮之后的音频轮次会补。
- 「重看序章」入口暂未做（设置面板下半区已排满三按钮 + 返回，需重排版面）；已记入轮次 9 的引导盘点。
- 八境志异只在世界志/秘境卡/主界面目标里露出，**战斗前后还没有剧情**（轮次 7 做"首领前故事 + 战后余韵"）。

### 2.4 下轮计划（轮次 3）

轮次 3 主问题：**战斗手感薄**——伤害只有血条变化，技能命中没有数字/顿帧/连携提示，打完不知道自己强在哪。计划：伤害飘字 + 暴击/克制强调 + 连携触发提示 + 技能名条与结算小结，让核心玩法"打起来有反馈"。

---

## 轮次 3｜打击感：让"打上去"有重量（核心玩法）

**现状缺口**：战斗已有落血、暴击飘字「暴 N」、连携提示、受击闪白，但——
1. **主问题｜命中没有"重量"**：任何一下打上去画面都一样，重击和刮痧在体感上没区别。
2. 副｜**敌方施法完全看不见**：`cast_start` 只在"施法者是玩家角色"时提示，敌方（含首领大招）静默出手，玩家只能看到自己突然掉血。
3. 副｜飘字只有一档：暴击/普通/持续伤害除了颜色，大小与动感一样，看不出轻重。
4. 副｜**残血没有任何警示**：血条细到 44px，血量掉到红线也不知道，等反应过来已经结算了。
5. 副｜结算只有"胜利 + 奖励"，不知道自己这局打得怎么样。
6. 副｜（顺带修）**结算那一帧若正抖屏，战场会一直歪着**——没有收招逻辑。

### 3.1 本轮改动（全部在表现层，sim 规则一行未动）

| # | 类别 | 改动 |
|---|---|---|
| 主 | 打击感 | **顿帧**：暴击/重击（单次伤害 ≥ 目标 12% 生命）时把 sim 步进压到 0.18 倍、70ms 内回弹；只动步进倍率，玩家的 ×1/×2 不受影响 |
| 主 | 打击感 | **震屏**：背景 + 战场挂进 `_shake_root`，按轻/重/暴击分三档抖动（2.0 / 7.0 / 4.5 振幅），HUD 与飘字不跟着晃；结算时 `_stop_shake()` 强制归位 |
| 副 1 | 可读性 | **敌方施法提示**：非角色施法显示「敌方 · 技能名」，首领技显示「首领技 · 技能名」并先晃一下当预警 |
| 副 2 | 可读性 | **伤害飘字分层**：暴击最大最亮（FS_LG+4，「暴 N」）、重击次之（橙红）、普通居中、持续伤害最小最淡；都带 0.7→目标值的弹出 |
| 副 3 | 可读性 | **低血警示**：角色血量 <25% 时屏幕边缘红晕脉动（120×200 径向渐变 + 线性过滤）+ 首次补一句「危急 · 血量过低，补药或撤退」 |
| 副 4 | 战报 | 结算面板加一行：用时 / 最高单击 / 我方输出 / 我方承伤（表现层统计） |
| 副 5 | 修复 | 结算帧收招（顿帧归零 + 震屏归位） |

### 3.2 验证

- `VerifyBattleScene` 扩了 8 条断言：顿帧触发/归零、震屏位移/归位、敌方施法提示文案、战报最高单击与承伤、**低血层挂点必须在根节点而不能混进飘字层**（飘字层有"结算后应清空"的断言）。
- 用例里把 `speed` 置 0 冻住 sim：只验表现层，避免真实伤害持续插进来让断言飘。第一次跑正是因此暴露了"结算后顿帧/震屏不收敛"的真 bug（已修）。
- 全量回归：`VerifyBattleScene / VerifyMapScene / VerifyRouteScene / verify_battle / verify_route / VerifyLore / VerifyGameHome / VerifyPerf` 全绿。
- 截图复查：`battle_low`（新增用例，hp_override 压到 10% 看红晕）、`battle` 两张通过；红晕上限从 0.70 收到 0.52（够警觉但不糊屏）。

### 3.3 下轮计划（轮次 4）

轮次 4 主问题：**主城只是个布景**——NPC 只会说一句随机台词、建筑只给一次性活动，玩家没有"每天回来有事做"的理由。
计划：NPC 委托（日刷新，接取 → 打怪/带物 → 回城交付领奖），把"主城 → 出征 → 主城"的循环真正接上，并让 NPC 台词跟委托状态联动（接了/完成/交付出不同的口吻）。

---

## 轮次 4｜每日委托：给"回主城"一个理由（把主城 → 出征 → 回城串成环）

**现状缺口**：
1. **主问题｜主城是布景**：NPC 只有随机轮换的台词，建筑只给一次性活动；出征回来除了数值增长，没有"有人等着你回来"的钩子。
2. 副｜**NPC 台词是死的**：不管你来干嘛、办没办完，永远是那几句。
3. 副｜**日常没有指引**：轮次 2 补了"主线目标"（一片大陆 → 首领），但"今天该做什么"仍然空白。
4. 副｜**征战的收获没有仪式**：奖励直接进钱包，没有一个"回城交差、对方应声"的落点。

### 4.1 本轮改动

| # | 类别 | 改动 | 文件 |
|---|---|---|---|
| 主 | 表 | `data/quests.json`：8 条委托（`slay` 击杀 / `clear` 讨伐首领 / `deliver` 上交道具），日刷 2 条；奖励覆盖金/经验/远征币/荣誉/魂晶 | `data/quests.json`（新） |
| 主 | 状态机 | `G.quest`（day / offer / active / claimed）：今日牌按「日期 + 等级」**确定性**抽取（同一天反复读不跳、改档也不乱）；**跨日未交付作废**（今日事今日毕，不堆任务墙）；接取门禁（不在今日牌上的接不了、交过的不能再接） | `G.gd` |
| 主 | 界面 | 新增**委托板** `QuestPanel`：今日委托列表（发布人+头衔 / 目标 / 状态 / 奖励一行小字 / 操作按钮），接取时弹出发布人的原话，交付时 toast 报赏 | `src/ui/QuestPanel.gd/.tscn`（新） |
| 副 1 | 入口 | **主城左上常驻小签**「委托 · x/y 已交付」：点开委托板；面板关闭自动刷新（刚交付完的进度立刻回写） | `CityScene.gd` |
| 副 2 | 台词联动 | 发布人的第一句台词**按委托状态变口吻**：未接→主动提起 / 进行中→提委托名 / 可交付→"看你这身风尘" / 已交付→致谢；与原有轮换台词不冲突 | `CityScene.gd`、`G.gd` |
| 副 3 | 战斗联动 | 战斗胜利上报 `quest_report("slay", theme, 1)`（只算**对应秘境**的击杀）；通关首领走 `on_world_cleared` 上报 `clear`；做满时战斗里就地弹「委托办妥 —— 回城交付」 | `MapScene.gd`、`G.gd` |
| 副 4 | 道具 | 上交类委托需要道具（宠物粮 / 扫荡券）：`ITEM_NAMES` 显示名表 + `grant_item` / `consume_item`（交付时校验并扣物） | `G.gd` |
| 副 5 | 回归 | `VerifyQuests`：表完整性（发布人必须存在于 city.json）/ 日刷新与跨日重刷 / 三类推进与交付 / 奖励入账 / 门禁 / 存档往返 / NPC 台词四态 | `tools/VerifyQuests.gd/.tscn`（新） |

### 4.2 验证

- 本轮补跑：**18 项 headless 全绿**（轮次 4 提交时 17 项；`VerifyQuests` 覆盖 A~I 段共 40 余条断言）。
- 截图复查：`quests`（一条可交付 + 一条进行中，两种状态同屏）、`city`（左上「委托 · 0/2 已交付」小签）——文案未裁切、按钮态正确。
- 存档：`prog.quest` 写入读档（老档缺省为空 → 首读即重刷）。

### 4.3 未解决 / 风险

- 今日牌**只能接不能换**（没做"换一批"消耗资源的钩子）——克制设计，但若玩家遇到两条做不了的委托会卡一天，留待观察。
- 委托发布人沿用主城既有 NPC，**没做"布告板"实体建筑**（左上小签即入口）。
- NPC 台词只有口吻变化，**没有因委托展开的支线对话**（轮次 7 的"首领前故事"可顺带补）。

### 4.4 下轮计划（轮次 5）

轮次 4 交付后盘点：委托把循环接上了，但整个游戏**一点声音都没有**——`assets/audio/` 只有 6 首占位 BGM，`ui_click` 在轮次 1 时随工作区删除，所有 `Audio.sfx(...)` 调用静默；按钮无回响、战斗无打击声、领赏无入账声。用户总目标"减少 AI 感"在这里最刺眼。
计划：**程序合成一套音效（无版权依赖）+ 音效池 + 全流程接线**，让每个交互都有"听得见的反馈"。

---

## 轮次 5｜"听得见的反馈"：从全游戏静音到 18 件音效

**现状缺口**：
1. **主问题｜全游戏静音**：唯一接线的 `ui_click`（GameHome 一处）因素材不在而静默；`G.gold_button` / `info_button` 号称"已挂点击音"实际未挂（交接文档记错了）。
2. 副｜**单一 SFX 播放器**：`Audio.gd` 只有 1 个 `AudioStreamPlayer`——快速连点/连击时，后一个音会把前一个掐掉，听感"断"。
3. 副｜**同一音反复播放完全一致**：短音效复读极易听出"电子味/机械感"。
4. 副｜**接线面几乎为零**：翻页、浮层开关、战斗命中、奖励入账、委托接取/交付、创角确认……全都没接。
5. 副｜**战斗打击感有视觉没听觉**：轮次 3 做了顿帧/震屏/飘字三档，声音上轻/重/暴击毫无区别——视觉给了重量的暗示，耳朵却收不到。
6. 副｜**没有音效回归**：素材缺失（如误删 wav）、连播、静音开关这些都不会被任何测试发现。

### 5.1 本轮改动

| # | 类别 | 改动 | 文件 |
|---|---|---|---|
| 主 | 素材 | `tools/make_sfx.py`：numpy **程序合成 18 件音效**（44.1kHz/16-bit WAV，可反复生成）。设计走木质/羊皮纸/克制：UI 七件峰值 0.55、奖励三件 0.75、战斗八件 0.80；打击音用"闷击（基频下滑）+ 噪声瞬态"、金币用非谐波金属泛音、胜利用铜管谐波堆叠 | `tools/make_sfx.py`（新）、`assets/audio/*.wav` ×18 |
| 主 | 播放层 | `Audio.gd` **音效池**：8 个 `AudioStreamPlayer` 逐次轮转（连点不掐断）；每次播放 **±3% 随机音高**（复读不成机械感）；新增 `SFX_NAMES` 清单 / `preload_all()` / `sfx_path()` / 自检访问器 | `Audio.gd` |
| 主 | 接线 | **全局按钮**：`G.gold_button`（含技能/药剂/换宠/速度等全部金按钮）与 `G.info_button` 按下即响 `ui_click`；PageDeck 翻页 `ui_page`（instant 初始化不响） | `G.gd`、`PageDeck.gd` |
| 副 1 | 接线 | **浮层开关音**：GameHome 六个入口 + CityScene 四个浮层 + 养成六条支线，开 `ui_open` / 关 `ui_close`；委托板、NPC 对话、篝火弃词条、撤离确认同 | `GameHome.gd`、`CityScene.gd`、`GrowthPanel.gd`、`QuestPanel.gd`、`MapScene.gd` |
| 副 2 | 接线 | **战斗听觉三档**：命中按 crit/heavy/普通分别 `hit_crit` / `hit_heavy` / `hit_light`（与顿帧、震屏同档触发）；施法 `skill_cast`、首领抬手 `boss_warn`、低血首次 `low_hp` 心跳、结算 `victory` / `defeat`（结算音不抖音高——是"定局"） | `BattleScene.gd` |
| 副 3 | 接线 | **奖励入账音**：升级 `level_up`（不抖音高）、抽卡开卡（出金换更亮的音）、兑换/扫荡/宝箱/商队 `coin`、篝火/赠礼/委托交付 `reward`、词条选择 `ui_confirm`、所有"走不通"（荣誉不足/券不足/未解锁/名字为空）统一 `ui_locked` | `G.gd`、`GachaPanel`、`ExchangePanel`、`DeployPanel`、`MapScene`、`QuestPanel`、`TraitPicker`、`CreateRole` |
| 副 4 | 加载 | 加载页把 18 件音效随贴图一并预热（进度条照走）——**第一次点击就该有回声** | `LoadScreen.gd` |
| 副 5 | 回归 | `VerifyAudio`：18 件素材**缺一个就红** / 池逐次轮转 / 20 次播放音高多样性 / 静音早退 / BGM 同名不重播·换曲·缺曲静默降级 / 音量 API | `tools/VerifyAudio.gd/.tscn`（新） |

### 5.2 验证

- **18 项 headless 回归全绿**（新增 `VerifyAudio`；此后误删任一 wav 都会直接红）。
- 截图复查：`home` / `battle`（+ 轮次 4 的 `quests` / `city`）——纯音频轮，画面零变化，无视觉回归。
- 过程发现并修：验证脚本里 `"±10% 内"` 触发坑 8（格式串里字面 `%` 紧跟非格式字符）→ 改全角 `％`。
- 音效素材生成 → `--import` 一步到位；加载页预热队列 +18 项后仍是 1.2s 级，`VerifyPerf` 预算全绿。

### 5.3 未解决 / 风险

- **实际听感只能人工验收**：headless 只能守"素材在、能播、不掐断、音高有扰动"；音量配比（UI 0.55 / 战斗 0.8）建议戴耳机过一遍再微调 `make_sfx.py` 的峰值参数。
- BGM 仍是 6 首临时件（曲风不搭），程序合成音乐质量不可控，**留待人工挑选/生成后同名替换**（来源清单在 `assets/audio/README.md`）。
- 少量交互点未接音：世界/图鉴的卡片点击（有 `ui_click` 兜底）、路线图节点选择、战斗技能按钮（同金按钮有 `ui_click`）——机制上多数已被全局按钮覆盖。
- 8 播放器池在极端连点下的混音叠加未做限幅，理论上有削波风险（振幅都很小，实测未闻）。

### 5.4 下轮计划（轮次 6）

候选按价值排序：①战斗外剧情演出（首领前对峙 + 战后余韵，数据全在 `lore.json`，直接命中"丰富剧情背景"）；②世界图志/图鉴大卡按需建卡（轮次 1 遗留：首次打开 119ms）；③"重看序章"入口（设置面板重排）。
**定轮次 6 主问题：战斗前后剧情演出**——现在的战斗只有"数值对撞 + 打击感"，玩家不知道对面的首领是谁、为什么挡路；八境志异里每片大陆的"首领来历"已经写好，只差在战斗前/后"演出来"。

---

## 轮次 6｜首领前「对峙」与战后「余韵」：把八境志异演出来

**现状缺口**：
1. **主问题｜战斗没有前因后果**：从出征到开打之间没有任何演出——玩家不知道对面的首领是谁、为什么挡路；打完也只有"首领陨落"一行 toast。轮次 2 写好的八境"首领来历"只躺在图鉴里静态可读，游玩主线关键节点（首领战）反而最安静。
2. 副｜**世界观有文本、没"时刻"**：叙事只出现在序章（入城前）与图鉴（主动翻），出征全流程没有一处"剧情发生的瞬间"。
3. 副｜**"重复劳烦"风险**：演出若每次开战都放，会拖累刷图——必须"首次全演、复战免打扰"。
4. 副｜**没有剧情回归**：某片大陆漏写对峙文案、或存档门禁坏了，不会被任何测试发现。

### 6.1 本轮改动

| # | 类别 | 改动 | 文件 |
|---|---|---|---|
| 主 | 剧情 | `data/lore.json` 八境各补两段演出文本：**`boss_intro`**（碑前对峙 3 行：环境 / 首领开口 / 主角应话）与 **`boss_outro`**（碑灵低语 3 行：临终 / 印记 / 图志记一笔）。全部沿用既有文风与设定（森林之主是把自己种进树心的守碑人、雪原女巫"第一个听见碑里说话"、首座"活一半人"的账……） | `data/lore.json` |
| 主 | 演出 | 新增演出层 `StoryBeat`（字幕式）：全屏压暗 + **首领剪影**（战斗内同款精灵放大压暗；缺素材回退暗影）+ 地名小字 / 首领大字 + 台词**逐行淡入**；点任意处快进→再点结束；右上「跳过」；文案为空时**静默不演**（绝不拦流程） | `src/ui/StoryBeat.gd/.tscn`（新） |
| 主 | 接线 | MapScene：BOSS 战前演 `intro`（**首次**挑战该秘境首领时）；战后演 `outro`（**首次**击败时——解封传送阵在前、演出在后，演完回词条三选一） | `MapScene.gd` |
| 副 1 | 门禁 | "只演一次"落存档 `prog.lore_beats`（老档缺省未看）；演出期间冻结移动与 ESC 撤离 | `G.gd`、`MapScene.gd` |
| 副 2 | 素材 | 剪影零新素材：`G.theme_boss_id` 取 monster id → `res_tex`（与战斗内同一寻址）；`theme_boss_name` 改由 `theme_boss_id` 推导（消灭两处各写一份） | `G.gd` |
| 副 3 | 加载 | 演出层场景加入加载页预热（不在开战那一帧现编译） | `LoadScreen.gd` |
| 副 4 | 回归 | `VerifyLore` 扩 E 段：八境 intro/outro 每段 ≥2 行、只演一次门禁 + 存档往返、演出层能建能铺满能推进结束并回调 | `VerifyLore.gd` |
| 副 5 | 工具 | 截图新增 `story_intro` / `story_outro` 用例；`VerifyMapScene` / `VerifyRouteScene` 的 BOSS 用例先标记节拍已看（战斗机制直连，演出由 VerifyLore 覆盖） | `ShotRunner.gd`、`VerifyMapScene.gd`、`VerifyRouteScene.gd` |

### 6.2 验证

- **18 项 headless 回归全绿**（VerifyLore 扩到 5 段）。过程暴露 2 处必须适配的问题并修复：
  ① 测试在已释放实例上查询状态（`queue_free` 帧末真释放）→ 改同帧查询 + `is_instance_valid`；
  ② VerifyMapScene / VerifyRouteScene 的 BOSS 用例被"首次对峙演出"拦下（`_battle == null`）→ 用例先标记已看直连战斗。
- 截图复查：`story_intro`（森林之主 · 暗红对峙）、`story_outro`（亡灵君王 · 金灰余韵）——剪影、双行标题、三行台词、跳过与提示均在位，无裁切、无叠字。
- 存档兼容：老档无 `lore_beats` → 读档补 `{}`，"首战必看"对老玩家同样成立。

### 6.3 未解决 / 风险

- 演出是"静帧 + 字幕"：无立绘表情与运镜。后续若按 `docs/人物素材需求.md` 规格接入首领半身像，可升级为"半身像 + 台词"。
- `outro` 与"首次击败"绑定：首杀后未通关（中途撤离）也视为看过，不再重演——有意为之（不打扰）。
- "重看这段"入口未做，与"重看序章"一并排进引导盘点轮。

### 6.4 下轮计划（轮次 7）

轮次 6 把八境的"首领会说话"补上了，但**大卡面板的首次打开仍然卡**（轮次 1 就欠着的世界图志 ~119ms：8 张 SlideCard 一次性建完），且轮次 2/5 攒下两件"入口类"小事（重看序章、重看演出）。
**定轮次 7 主问题：大卡按需建卡（PageDeck 虚拟化）**——把"用到才建"做进组件层，图鉴/世界志/出征筹备一并受益；顺手在设置面板补"重看序章"入口（面板下半区需重排）。

---

## 轮次 7｜转场与"可回访"：消灭硬切，补上重看入口

**现状缺口**：
1. **主问题｜全项目场景切换是"硬切"**：23 处 `change_scene_to_file` 直切——主界面进主城、出征进路线图、序章入城……画面瞬变、没有过场，是最直观的"廉价感"来源（热门游戏都有转场）。
2. 副｜**连点会切两次场景**：直切无防抖，快按两下能触发两次切换。
3. 副｜**转场窗口期玩家仍可操作**：切场景的半秒里点击仍会落到旧场景上。
4. 副｜**"重看序章"没有入口**（轮次 2 遗留）：想重温只能开新档；设置面板下半区已排满，需重排版面。
5. 副｜**分页圆点只是装饰**：PageDeck 圆点不能点击跳页（手游习惯是点圆点直达）。
6. 副｜**没有防回退**：以后新增跳转若直接写 `change_scene_to_file`，转场被绕开也不会有任何测试发现。

### 7.1 本轮改动

| # | 类别 | 改动 | 文件 |
|---|---|---|---|
| 主 | 转场 | **`G.go(scene)` 统一转场**：遮罩淡入 0.16s → 换场景 → 停留 0.06s → 淡出 0.16s；遮罩深棕黑（非纯黑，与羊皮纸调性一致）+ CanvasLayer 128（压过 GM 控制台）；tween 挂在 autoload 上，换场景不中断 | `G.gd` |
| 主 | 转场 | **23 处硬切全改走 `G.go`**：Main(5) / Title(1) / Login(2) / CreateRole(3) / Prologue(1) / LoadScreen(2) / GameHome(5) / CityScene(2) / RouteScene(3) / SettingsPanel(3)；战斗"无人接管"兜底保留直切并注明（headless 冒烟路径） | 10 个文件 |
| 副 1 | 防抖 | 转场期间 `ui_blocked=true` + 遮罩吃输入 + `can_go()` 预检（忙碌中或路径不存在直接拒绝并 push_error）——**连点两次只切一次** | `G.gd` |
| 副 2 | 入口 | 设置面板重排：新增**「重看序章」**（与重建角色/回标题一行），重置存档与提示下行、返回下移——补上轮次 2 留下的口子 | `SettingsPanel.gd` |
| 副 3 | 交互 | PageDeck **圆点可点跳页**（14×14 命中区包住视觉 7px 圆点） | `PageDeck.gd` |
| 副 4 | 回归 | 新增 `VerifyTransit`：全场景路径可达（防打错字）/ `can_go` 放行与拦截 / 忙碌防重入 / 遮罩懒创建·不吃输入·压最上层 / **源码扫描防回退**（src/ 下除白名单不许出现 `change_scene_to_file`） | `tools/VerifyTransit.*`（新） |

### 7.2 验证

- **19 项 headless 回归全绿**（新增 `VerifyTransit`）。防回退扫描**第一次跑就抓到漏网**：替换 CreateRole 时因缩进不同，`replace_all` 只命中一处（ESC 分支漏改）——这正是"扫描源码"这条断言的价值，已修。
- 截图复查：`settings`（四钮两行 + 返回，无重叠）、`worlds`（圆点指示器正常、卡面不受命中区影响）。
- 遮罩状态由断言守住：平时 `mouse_filter == IGNORE` 且全透明、层级 ≥120；`VerifyPerf` 的耗时预算不受影响（它不走转场）。

### 7.3 未解决 / 风险

- 转场只有"淡入淡出"一种：无方向性与场景类型区分（入城 / 出征 / 回标题同款）。先统一（克制）；若要"出征更庄重"，可在 `G.go` 加参数（记入后续"过场演出"轮）。
- 遮罩合计 ~0.38s，比硬切慢半拍——换来"画面有呼吸"，观感收益大于等待成本。
- 大卡按需建卡（世界图志首次 ~119ms）顺延为轮次 8 主问题（本轮主问题按"感知强度"改选转场，已在计划中重排并说明）。

### 7.4 下轮计划（轮次 8）

**定轮次 8 主问题：大卡按需建卡（PageDeck 虚拟化）**。

---

## 轮次 8｜大卡按需建卡

**现状缺口**：大卡面板"一次建完所有卡"（轮次 1 欠账）：世界图志 8 张全建首开 119ms，图鉴 86ms。

**主改动**：

- PageDeck.set_factory：卡用到才建，go 时建目标页+相邻页
- WorldPanel/CodexPanel/DeployPanel 三处改工厂装页
- VerifyPerf 浮层预算 130→100ms，加"世界图志只建≤3 张"断言
- VerifyGameHome 出征断言改口径：先查首开少量，再翻遍建满 8/4/8

**效果（headless 实测）**：世界图志首开 119→47ms；图鉴 86→47ms。

**验证**：19 项 headless 全绿；截图复查 worlds/codex/deploy 三张，首屏正常。

**风险**：出征三页签仍在切页签时重建（非主路径，暂不优化）。

### 8.5 下轮计划（轮次 9）

**定轮次 9 主问题：打怪掉落表落地**——现在怪物只掉金币/远征币，材料只能兑换；

---

## 轮次 9｜打怪掉落表落地

**主问题**：怪物只掉货币不掉材料，养成的强化石/精炼石/宝石只能荣誉兑换，「刷图」无产出感。

**改动**：

- `data/drops.json`（新）：按节点档位三档掉落表（normal/elite/boss），字段 item/chance/min/max；
  普通掉强化石/宠物粮，精英保底强化石+概率精炼石/突破晶，首领保底精炼石+概率资质果/锁定符。
- `G.roll_drops(tier)`：掷表返回 [{item,n}]（空表静默）；`TableCache.drops_config()`。
- MapScene 战胜后 `_grant_drops`：掷表 → grant_item → 「拾获：xxx ×n」一条 toast（无掉落不吵）。
- 新增 `VerifyDrops`：三档表完整性（item 已登记/概率与数量区间合法）、必出条目多轮命中、掉落入账。

**验证**：20 项 headless 全绿（回归清单至此 15 场景 + 5 脚本）。

**风险**：掉落只在战斗胜利时结算，扫荡不产出材料（有意：扫荡只吃货币收益）。

### 9.5 下轮计划（轮次 10）

**定轮次 10 主问题：PVP 竞技场（首版）**。

---

## 轮次 10｜演武场首版：傀儡切磋 + 段位分

**形态取舍**：真·镜像对战需要 BattleSim 支持敌方角色单位（大改），首版走「演武傀儡」：
对手是按玩家等级生成的自定义单位，核心循环（选对手 → 切磋 → 段位分）先行，镜像留后续。

**改动**：

- BattleSim：`enemy_cfg.custom_mon` 支持"自定义对手"（不走怪物池，直接给 name/base/skills）。
- `G`：`arena` 状态（score/wins/losses，入存档）+ `make_arena_foe(level)`（数值随等级缩放）
  + `arena_result(win)`（胜 +18~26 / 负 -12 保底 0）+ `arena_rank()`（铜/银/金印）。
- `ArenaPanel`（新）：段位/战绩一行 + 对手预览 + 换对手/开始切磋；战斗为覆盖层（BattleScene），
  结算回面板 toast 段位分变化。主界面右列新增「竞技」入口（7 项，间隔 84→78）。
- 新增 `VerifyArena`：傀儡数值随等级递增、BattleSim custom_mon 单位正确、段位分加减与保底、存档往返。

**验证**：21 项 headless 全绿（新增 VerifyArena，回归清单 16 场景 + 5 脚本）；
截图 `arena` 复查（匾与主界面"远征"匾错位排布，面板内容无遮挡）。

**风险**：傀儡用程序圆体贴图（mon_arena_dummy 无素材）；真镜像对战（敌方角色单位）
需要 BattleSim 扩展，已列后续。演武场战斗不给词条三选一（PVP 不该滚雪球，有意为之）。

---

## 十轮收官小结

10 轮自动迭代全部完成。总目标对照：
- **参考热门游戏**：每日委托（日刷闭环）、演武场（段位分）、抽卡/结算反馈音——主干循环齐了。
- **优化界面与玩法**：加载页预热、按钮命中修复、大卡虚拟化（首开 119→47ms）、统一转场、圆点跳页。
- **减少 AI 感**：18 件程序合成音效（点击/命中/奖励不再无声）、随机音高防机械复读、转场消灭硬切。
- **丰富剧情背景**：世界志 + 序章 + 主线目标 + 八境志异 + 首领前对峙/战后余韵演出。

回归资产：16 个场景级 Verify + 5 个脚本级 verify（共 21 项），另有 ShotRunner 30+ 截图用例。
让「刷图」产出材料，把战斗与养成 6 线真正接上（数值进 data 表）。

---

## 轮次 11｜去 AI 感 UI 规范 + 头像系统

**现状缺口**（按 `docs/去AI感提示词.txt` 逐条对照后的实测）：

1. **主问题｜"AI 感"集中在尺寸与分区**：同一层级的按钮高度到处不一样（38 / 40 / 44 / 52 混用）；
   面板里文字直接铺在羊皮纸上，没有"信息带"分区；抽卡页顶部还挂着冷色**蓝城堡横幅素材**，压在暖色纸上像贴错了图。
2. 副｜**玩家形象无处体现**：登录页与主界面只有一个写死的职业头像，不能换图。
3. 副｜**主城建筑是纯色块**：玩家看不出哪栋是祭坛（签到）、哪栋是兵营，功能靠猜。
4. 副｜**两个 autoload 编译期互相引用**：`Audio` 直接读写 `G.audio`，音量保存还借道 `G.save_game()`。

**改动**：

| # | 类别 | 改动 | 文件 |
|---|---|---|---|
| 主 1 | 规格 | 新增「UI 规格（§6 有限尺寸）」常量：按钮三档 `BTN_L(190×52) / BTN_M(148×44) / BTN_S(120×38)`、图标三档 `ICON_RAIL / ICON_WALLET / ICON_MARK`——页面只准选档，不再逐页手调宽高 | `G.gd` |
| 主 2 | 组件 | `G.ghost_button()`（幽灵按钮）、`badge_dot()`（红点）、`_banner_text() / _button_text()`（匾额与按钮字距）、`_bind_press_feedback()`（按压反馈统一），各页面改吃同一口径 | `G.gd`、`Login.gd`、`GameHome.gd` |
| 主 3 | 头像 | 头像系统：`G.import_avatar(path)`（**居中裁方 → 缩到 256×256 → 存 PNG**）、`use_role_avatar(id) / use_custom_avatar()`、`avatar_texture()`；新增存档字段 `avatar_custom` / `avatar_use_custom`（选回职业头像时保留已上传图） | `G.gd` |
| 主 4 | 新面板 | `AvatarPanel`（新）：职业头像卡片一排（当前使用态高亮）+ 上传本地图（`FileDialog`）+ 恢复职业头像 | `src/ui/AvatarPanel.gd`（新） |
| 副 1 | 入口 | 登录页与主界面左上角头像可点开换头像；`_open_avatar_panel()` / `_close_overlay_with_escape()`（浮层内 ESC 只关浮层，主界面 ESC 才开设置） | `Login.gd`、`GameHome.gd`、`SettingsPanel.gd` |
| 副 2 | 抽卡页 | 顶部改「本期主打」**内嵌信息带**：主打宠物图 + 名 + 稀有度 chip + 保底数字 + 进度条 + 下一档提示 + 规则 ⓘ；余额条同样内嵌成带（冷色横幅素材撤下） | `GachaPanel.gd` |
| 副 3 | 主城 | 功能建筑挂小图标（祭坛 / 兵营 / 档案馆 / 库房）+ 悬停显活动名，色块不再让人猜用途 | `CityScene.gd` |
| 副 4 | 修 Bug | `ArenaPanel` 对手卡由 `PanelContainer` 改 `Panel`：**容器会把图标与两行字全部拉伸铺满整卡**，实测"对手：演武傀儡…"与副标题叠字；顺手去掉与名字里重复的 `Lv.N` | `ArenaPanel.gd` |
| 副 5 | 架构（含返工） | 拆掉 `G` ↔ `Audio` 两个 autoload 的**编译期双向引用**：`Audio` 音量读写改走 `_game_state()`、`G` 播声改走 `_sfx()`，两侧都只在运行时取节点；`VerifyAudio` 加源码扫描守则锁死这条规则。返工原因见下方「轮次 11 补丁」 | `Audio.gd`、`G.gd`、`tools/VerifyAudio.gd`、`SettingsPanel.gd` |
| 副 6 | 建角 | 建角完成时把 `avatar_id` 落定为所选职业，头像口径从第一帧就一致 | `CreateRole.gd` |
| 副 7 | 回归 | 新增 `VerifyAvatar`（导入裁方 / 尺寸 256 / 职业与自定义切换 / 存档往返）；`ShotRunner` 加 `avatar` 用例并支持从主页打开换头像浮层 | `tools/VerifyAvatar.gd`（新）、`tools/ShotRunner.gd`、`tools/VerifyGameHome.gd` |

**验证**：22 项 headless 全绿（17 场景级 + 5 脚本级，新增 `VerifyAvatar`，`VerifyAudio` 加循环依赖扫描）；
截图复查 `login` / `home` / `avatar` / `gacha` / `arena` 五张——修掉对手卡叠字与重复等级后重截确认，按钮三档在各页一致、信息带无压字。

**风险**：上传头像只做本地文件导入（无裁剪框，口径是居中裁方）；`user://avatars/` 不在云存档内，换设备需重新上传——有意为之。

### 轮次 11 补丁｜G ↔ Audio 循环依赖（编辑器满屏红字）

**现象**（用户在编辑器里看到）：

```
ERROR: res://src/ui/AvatarPanel.gd:28 - Compile Error: Identifier not found: Audio
ERROR: res://src/autoload/G.gd:409 - Compile Error: Identifier not found: Audio
ERROR: res://src/ui/ArenaPanel.gd:22 / GachaPanel.gd:499 / GameHome.gd:53 - 同上
```

**根因**：`G` 与 `Audio` 都是自动加载器，本轮同时出现两个方向的**编译期**引用——
`G.gd` 里写着 `Audio.sfx("ui_click")`（按钮按压反馈），`Audio.gd` 里还写着 `G.audio[…]` / `G.save_game()`。
编译器互等对方先编译 → `Audio` 这个全局标识符解析失败 → 凡是引用 `Audio` 的脚本**整片**报
`Identifier not found: Audio`（只在编辑期解析完整项目时暴露，headless 跑单场景恰好走的是另一条路径，所以没抓到）。

**修法**（两侧都改成运行时取节点，彻底断环）：

| 侧 | 旧写法 | 新写法 |
|---|---|---|
| `Audio.gd` | `G.audio["bgm"] = v` / `G.save_game()` | `_game_state()` → `get_node_or_null("/root/G")`，再 `get/set/call` |
| `G.gd` | `Audio.sfx("ui_click")` ×2、`Audio.sfx("level_up")` | `_sfx(name, jitter)` → `get_node_or_null("/root/Audio")` 再 `call("sfx", …)` |

**防回退**：`VerifyAudio` 新增 I 段源码扫描——扫 `Audio.gd` 里的 `G.` 与 `G.gd` 里的 `Audio.`（跳过注释行、放过
`"/root/G"` 这类字符串），命中即 FAIL 并报出「文件:行号」。用历史版本校验过：旧 `Audio.gd` 命中 7 处，
当前版本 0 处。

**复盘**：这类"编辑期才炸"的问题，headless 分场景跑是不够的——补一条**整包编辑期导入**检查
（`godot --headless --editor --quit`）与**窗口模式实跑 4 个主界面**（`home/avatar/gacha/arena`），两者现在都是 0 错误。
