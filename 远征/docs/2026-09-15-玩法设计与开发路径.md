# 《远征》玩法设计与开发路径 v1.3（2026-09-15）

> **定位**：本文是 [2026-09-12-远征开发方案.md](2026-09-12-远征开发方案.md)（v6）的**玩法落地版**，把人物机制 / 战斗逻辑 / 宠物 / 抽奖 / 活动 / 图片清单 / 开发路径落到可执行规格。两文冲突时以本文为准（v6 仅作背景与历史决策参考，不再单独维护）。
> **v1.3 变更**：①全文与**图片提示词总库 v2.1**（343 条）逐系统对齐——每个玩法模块新增"素材对应"小节，写明提示词节号与素材 id（§0.4 总表可速查）；②§7 PVP 扩充为完整章节（模式矩阵 / 匹配房间 / 锁步同步 / 结算段位 / 渐进方案）；③§10 开发路径重排定稿——**先单机完整闭环（阶段 0~4），再 PVP 按模式渐进（1v1→2v2→3v3，阶段 5）**，每阶段绑定素材依赖。
> **v1.2 变更**：新增**探索大地图层**（人物自由移动 + 地图上遇敌开战，§2.7）——素材体系从整张竖屏背景改为 tile+散件分层拼装，提示词见 [2026-09-15-图片提示词总库.md](2026-09-15-图片提示词总库.md) §1。
> **v1.1 修正**：交叉自查发现 16 处问题（见 §13），攻击规则 / 敌方编成 / 局内延续 / PVP 归一化 / 武器分系 / 词条口径 / 宠物属性均已定稿修正——**开工前必读 §13**。
> 素材明细单一事实来源：[2026-09-15-美术素材总清单.md](2026-09-15-美术素材总清单.md)；**生成用提示词单一事实来源：[2026-09-15-图片提示词总库.md](2026-09-15-图片提示词总库.md) v2.1（343 条，每条自带规格/风格/背景/禁止项）**。
> **For agentic workers**：所有任务可勾选（`- [ ]`），每个模块含数据表 schema 与验收标准，逐条执行逐条验证。

**全局设计总纲**：复杂度敞开加，挫败感继续克制——失败无惩罚、重复不浪费、概率全公示、保底永久累计。

**技术不变量**：Godot 4.7.2 / Compatibility / 480×800 竖屏 / 全 JSON 表驱动 / PVE 本地结算 / PVP 锁步帧同步。UI 一律走 `G.gd` 主题工厂（暖棕+羊皮纸+金，字号阶梯 13/16/18/22/30/56），文字严禁烧进图片。

---

## 0. 全局逻辑与对应关系

### 0.1 端到端玩法逻辑链（一张图理清全游戏）

```
【局外循环 · 主界面】
  主界面(bg_main)
    ├─ 出征 → 选层(8主题) → 选人物(4人,roles.json) → 选出战宠+替补(pets.json)
    ├─ 养成6线(§6)：等级天赋/装备/宠物/技能书/坐骑/称号 → 战力提升
    ├─ 抽奖(§4)：祭坛(宠物池·灵魂石) / 秘库(装备池·金币)
    ├─ 活动(§5)：签到/兑换（配置驱动）
    └─ PVP(§7)：排位/休闲 → 1v1/2v2/3v3

【局内循环 · 一局远征（5~10分钟）】
  路线图骨架(nodes.json: 起点→3层×3选1→BOSS)
    → 进入节点 = 落地为探索大地图区域(maps.json: tile+散件+怪物刷新+出口传送阵)
    → 大地图自由移动(摇杆/点击寻路) → 怪物警戒追击 → 接触开战
    → [战斗](§2站位制): 自动普攻+5技能手动+换宠+托管
    → 胜利回地图(怪消失/掉落) → 三选一词条(traits.json) → 找传送阵 → 下一节点
    → (HP跨节点延续/能量buff清空/词条保留 §2.6)
    → BOSS节点 → 局结算: 材料产出(金币/远征币/灵魂石/材料)

【接口】远征产出 → 养成消耗(金币强化/远征币精炼/灵魂石抽宠/荣誉PVP)
        已通关层 → 扫荡(70%收益) → 快速积累
```

**三条经济闭环**（货币产消总表见 §6）：
1. 战斗闭环：局内词条构筑（随局重置）→ 局外数值养成（永久）
2. 抽奖闭环：远征产出货币（灵魂石/金币）→ 抽奖 → 宠物/装备 → 战力 → 更高层
3. PVP 闭环：技巧对局 → 荣誉 → 称号/限定材料 → 差异化（不加深数值差距）

### 0.2 开发顺序总纲（v1.3 定稿：单机先行，PVP 渐进）

```
阶段0 工程奠基 → 阶段1 战斗竖切 → 阶段2 远征循环(大地图) → 阶段3 养成+抽奖
→ 阶段4 单机深化(深渊/无尽/词条轮换)  ←—— 单机完整闭环，可独立上线测试
→ 阶段5 PVP渐进: 5A 账号+确定性改造+中继 → 5B 1v1 → 5C 2v2 → 5D 3v3 → 5E 赛季
→ 阶段6 社交 → 阶段7 打磨发布
```

单机闭环先行的理由：①PVE 本地结算无网络依赖，验证核心玩法最快；②确定性改造（5A）复用阶段 1 的战斗内核（§2.1 时序本就按可复现种子设计）；③ PVP 1v1 只需"1 个人物+1 宠"上阵，2v2/3v3 复用同一战斗内核仅扩展布阵显示——**战斗内核一次写对，四个模式全吃**。

### 0.3 单局状态机（玩家视角）

```
IDLE(主界面) → DEPLOY(选层/选人/选宠) → ROUTE(路线图选节点)
  → EXPLORE(大地图移动/交互) ⇄ BATTLE(遇敌开战)
       ↑↓  胜:回EXPLORE / 败:→SETTLE
  → BOSS_BATTLE → SETTLE(结算材料) → IDLE
任意 BATTLE 失败 → SETTLE（无惩罚，材料照给）
```

### 0.4 系统↔素材对应总表（提示词总库 v2.1 速查）

> 提示词总库共 343 条、16 个素材章节；本表是"玩法模块 → 素材章节 → id 前缀 → 数量"的索引，生成素材时按表索骥。

| 玩法模块 | 提示词节 | id 前缀 / 关键 id | 数量 | 使用场景 |
|---|---|---|---|---|
| 登录/选人/创建 | §1.5 | bg_login / bg_select / bg_create | 3 | 界面背景 |
| 主界面 | §1.5 | bg_main + icon_altar + icon_vault + mkt_logo | 4 | 出征/抽奖入口 |
| 路线图 | §1.5+§8 | bg_route + node_* ×8 + 连线(程序绘制) | 9 | 局骨架选节点 |
| 探索大地图 | §1.1~1.4 | tile_* ×24 / deco_* ×29(主题24+通用5) / 氛围4 | 57 | 自由移动/遇敌 |
| 战斗场景 | §1.5 | bg_battle_{forest,glacier,...} ×8 | 8 | 遇敌开战背景 |
| 战斗 HUD | §9.4 | ui_panel_battle(技能条底) + sk_* ×20(技能图标) | 21 | 5技能条 |
| 技能特效 | §5 | fx_{技能id} ×25(含通用5) | 25 | 技能命中表现 |
| 状态效果 | §6 | buff_* ×17 + sfx_* ×8 | 25 | buff图标+表现 |
| 三选一词条 | §9.1~9.3 | ui_panel_card + frame_* ×4 + school_* ×6 + icon_double_edge | 12 | 词条卡与流派 |
| 人物 | §2.1~2.3 | role_{zs,ck,fs,fz} ×4 + 姿势 ×12 + 装备部件 ×6 | 22 | 母图/换装/待机 |
| 宠物 | §3 | pet_* ×8 + *_evo ×8 | 16 | 参战+图鉴 |
| 怪物 | §4 | mon_* ×35(含 BOSS 5+光环2) | 35 | 大地图+战斗 |
| 抽奖 | §10 | gacha_card_* ×4 + fx_gacha_* ×2 + icon_altar/icon_vault + ui_panel_odds | 9 | 祭坛/秘库 |
| 活动框架 | §11 | ui_signin/exchange/notice + ui_banner_{event,gacha} | 5 | 签到/兑换/公告 |
| 养成 6 线 | §13.4 | ui_panel_{talent,equip,refine,mount,title,skillbook} | 6 | 各养成页底板 |
| 装备系统 | §13.3+13.5 | itm_{强化石,精炼石,龙鳞,洗练锁定符...} + slot_* ×6 | 30 | 装备页/物品 |
| 宝石 | §13.1 | gem_{atk,def,hp}_{1..5} | 15 | 镶嵌 |
| 货币 | §13.2 | cur_{gold,expedition,soul,honor} | 4 | 顶栏/产消 |
| 物品 | §13.3 | itm_* ×24 | 24 | 背包/消耗 |
| NPC | §14 | npc_{merchant,blacksmith,courier} | 3 | 商店/铁匠/信箱 |
| PVP | §12 | rank_* ×5 + ui_panel_{pvp_lobby,pvp_match,pvp_team,season} | 9 | 大厅/匹配/榜单 |
| 坐骑 | §15.1+15.2 | mount_* ×12 + fx_mount_summon | 13 | 坐骑页/骑乘 |
| 发行 | §15.3 | icon_app + mkt_{logo,splash,loading,store} | 5 | 商店/启动 |

---

## 1. 人物机制（4 人定稿）

### 1.1 框架

- **人物固定 4 名**，无人物卡池（抽奖不抽人，见 §5）：
  | id | 名字 | 职业 | 武器 | 定位 | 素材目录 |
  |---|---|---|---|---|---|
  | zs | 破军 | 战士 | 大剑 | 近战物理，能抗能打 | `image/role/zs/` |
  | ck | 穿杨 | 枪骑 | 长枪 | 中远程物理，灵动输出 | `image/role/ck/` |
  | fs | 霜语 | 法师 | 法杖 | 远程群伤+控制 | `image/role/fs/` |
  | fz | 晨星 | 牧师 | 圣锤 | 治疗辅助，可独立刷怪 | `image/role/fz/` |

- **出战规则**：PVE 远征局内 = 1 人物 + 1 出战宠物 + 1 替补宠物（见 §3.4 换宠）；PVP = 按模式 1/2/3 人上阵（宠物随人物参战）。**人物之间不切换**——选人即本局身份，跨局可选不同人物；等级/天赋/装备/技能书全局共用（武器分系例外，见 §6）。
- **属性 8 项**：HP / ATK / DEF / SPD（普攻间隔系数，roles.json 的 `speed`）/ CRIT / 能量(上限100) / 技能 CD / 控制抗性。无命中闪避、无物法分离。
- **普攻基准间隔 = 2.0s ÷ SPD**（SPD 为除数系数）→ 能量回充 = 20 × SPD / 秒。霜语 0.9 ≈ 9/s，穿杨 1.2 = 12/s——**能量经济分人物显著不同，阶段 1.8 必须分人物模拟**（霜语费用高+回充慢双重惩罚是否成立）。

### 1.2 成长公式（等级 1~60）

```
升到 lv+1 经验 = lv×100 + 2×5^(0.1×lv)
HP   = 100 + 40×(lv−1)
ATK  = 20  + 4×(lv−1)
DEF  = 10  + 2×(lv−1)
CRIT = 5% + 0.1%×lv
```

**装备外观 3 阶**（纯表现）：ATK 达 50 / 120 / 250 自动升阶，仅武器+甲两部件换装（见素材清单 A3）。

### 1.3 站位与攻击规则（策略核心，v1.1 定稿修正）

- 战场 20 站位 = 5 列 × 4 排，敌我各半场 5×2，靠中线一排为各自**前排**。
- **普攻目标规则**（消除 v6"远程可打后排"与夺魄"无视前排"的矛盾，定稿如下）：
  - **近战**（破军、晨星）：只能命中对方**前排**存活单位；敌方前排全灭时**战场推进**，可攻击任意存活单位（防僵局）
  - **远程**（穿杨、霜语、远程宠物）：可打**任意排**，默认目标 = 敌方最低血（被嘲讽时强制转火）
  - **指定排是技能特权，不是普攻特权**：夺魄=指定后排且无视 50% DEF（原"无视前排"附加值与远程普攻规则重复，已改）；倾泻=随机目标（含后排）；霜语 AOE=全体
- 位移词条/技能可改变站位（击退/换列）。
- 显示分层：PVE 只高亮有单位区域（紧凑）；PVP 1v1 紧凑 / 3v3 全场展开。格子逻辑两模式统一。

### 1.4 技能 20 条（id = `{人物id}_{拼音}`）

| 人物 | # | 技能 id | 名称 | k | cd | cost | 附加 |
|---|---|---|---|---|---|---|---|
| 破军 | 1 | zs_lieshan | 裂山 | 1.5 | 5 | 20 | 单体 |
| 破军 | 2 | zs_pozhen | 破阵 | 1.2 | 9 | 25 | 目标 DEF −20%×5s |
| 破军 | 3 | zs_huifeng | 回风斩 | 0.8 | 12 | 40 | 前排全体 |
| 破军 | 4 | zs_zhanhou | 战吼 | 0 | 10 | 20 | 强制目标攻击自己 3s（嘲讽） |
| 破军 | 5 | zs_duanzui | 断罪 | 2.5 | 15 | 60 | 目标 HP<30% 时 k→4.0 |
| 穿杨 | 1 | ck_lianzhu | 连珠 | 0.9×2 | 5 | 20 | 二段 |
| 穿杨 | 2 | ck_guanjia | 贯甲 | 1.0 | 8 | 25 | 目标 DEF −15%×5s |
| 穿杨 | 3 | ck_duopo | 夺魄 | 2.0 | 12 | 40 | 后排单体 + 无视 50% DEF |
| 穿杨 | 4 | ck_dunying | 遁影 | 0 | 10 | 20 | 自身"潜伏"3s（受击减半） |
| 穿杨 | 5 | ck_qingxie | 倾泻 | 0.6×3 | 15 | 60 | 随机目标三段 |
| 霜语 | 1 | fs_shuangren | 霜刃 | 1.8 | 5 | 25 | 单体（v1.1 改名：原"炽焰"与冰霜主题违和） |
| 霜语 | 2 | fs_shuanghuan | 霜环 | 1.0 | 10 | 30 | 全体+减速 20%×4s |
| 霜语 | 3 | fs_tianyun | 天陨 | 2.5 | 14 | 55 | 全体 |
| 霜语 | 4 | fs_midun | 秘盾 | 0 | 12 | 25 | 己方单体 20%HP 护盾 |
| 霜语 | 5 | fs_yanmie | 湮灭 | 3.0 | 18 | 70 | 对受控目标 k→4.5 |
| 晨星 | 1 | fz_shengyu | 圣愈 | 0 | 5 | 20 | 单体回复 15%MaxHP+0.5×ATK |
| 晨星 | 2 | fz_puzhao | 普照 | 0 | 10 | 35 | 全体回复 10%MaxHP+0.3×ATK |
| 晨星 | 3 | fz_jinghua | 净化 | 0 | 8 | 20 | 驱散单体负面（即时效果） |
| 晨星 | 4 | fz_zhudao | 祝祷 | 0 | 12 | 30 | 己方单体 ATK +20%×5s |
| 晨星 | 5 | fz_poxiao | 破晓 | 0 | 18 | 60 | 全体无敌 1.5s + 回复 8%MaxHP |

**连携表**（顺序触发，进 `combos.json`）：
- 破阵→断罪：k 额外 +1.0
- 霜环→天陨：对减速目标 +30%
- 祝祷→破晓：治疗量 +30%
- 潜伏(遁影)中释放夺魄：k +0.5

### 1.5 战斗内操作

5 技能条（能量不足灰显+CD 遮罩）+ 换宠按钮 + 托管开关。普攻全自动，技能全手动，托管 = 技能也自动按优先级。**换宠规则（v1.1 定稿）**：携带出战 1 + 替补 1；换宠 CD 60s（普通局 15~40s 基本用不到，是有意设计——换宠面向 BOSS/深渊局）；换上的宠物获**免死 1 次**（吸收一次致死伤害保留 1 HP）。

### 1.6 素材对应（人物，提示词总库 §2）

| 素材 | 提示词 id | 规格 | 用途 |
|---|---|---|---|
| 设定母图 | role_zs / role_ck / role_fs / role_fz | 512×512 透明 | 立绘基准+头像源 |
| 待机帧 | {人物id}_pose_idle ×4 | 64×96 帧序列 | 地图移动/战斗待机 |
| 移动帧 | {人物id}_pose_walk ×4 | 64×96 帧序列 | 大地图四向移动（上下左右镜像复用） |
| 攻击帧 | {人物id}_pose_attack ×4 | 64×96 帧序列 | 普攻/技能通用前摇 |
| 装备部件（外观 3 阶） | weapon_*/armor_* ×6 | 192×192 | ATK 50/120/250 自动换装（§1.2） |

**程序化策略**：3 阶外观 = 母图 + 部件叠加（Godot Node2D 分层），不做全套重绘；待机/移动/攻击三姿势全部从母图姿态派生，头像 = 母图裁切 96×96。

---

## 2. 战斗逻辑

### 2.0 战斗全流程状态机（遇敌 → 结算，逻辑主干）

```
ENTER(遇敌触发/接触判定)
  → 布阵快照: 敌方编成(§2.5)按站位表落位; 我方=人物+出战宠(按人物id固定列)
  → INTRO(1s): 背景bg_battle_{主题}淡入, 单位入场
  → LOOP(固定tick 1/30s, §2.1时序):
       BuffSystem → 施法队列(前摇) → TraitSystem → 普攻计时 → AI/玩家指令 → 死亡判定
       玩家输入(5技能/换宠/托管) → 指令校验(能量/CD/存活) → 入施法队列
       换宠: 替补入场(获免死1次) + 出战宠退场(本局不可再上, CD 60s)
  → 胜利条件: 敌方全灭 → VICTORY
  → 失败条件: 我方全灭(人物+宠) → DEFEAT
  → SETTLE: 战斗结果 → 掉落(itm_*/cur_*) → 回大地图(§2.7) 或 局结算(§2.6)
```

**指令优先级**（同一 tick 多输入）：换宠 > 技能 > 托管开关（换宠是即时快照操作，技能需排队前摇）。前摇 0.3~0.5s 的设计目标：①打击感（起手→命中两段表现）②PVP 联网延迟掩埋（§7）。

**战斗场景构成**（素材全部到位的完整战斗画面）：
- 背景：`bg_battle_{主题}` ×8（遇敌主题决定）
- 单位：人物（§1.6 姿势帧）+ 宠物（§3.4）+ 怪物（§4.4）
- HUD：`ui_panel_battle` 技能条底 + `sk_*` 技能图标 ×20 + `buff_*` 状态角标
- 表现：`fx_{技能id}` 命中特效 + `sfx_*` 状态特效 + 飘字（程序 Label，走 G.gd 字体）

### 2.1 一帧的时序（确定性要求，PVE/PVP 共用）

```
tick(固定 1/30s 步进):
  1. BuffSystem.tick()        # 持续 buff 计时/触发（流血跳字等）
  2. 施法队列推进             # 前摇 0.3~0.5s 倒计时，到点结算伤害（表现层可延迟播）
  3. TraitSystem 钩子          # on_tick
  4. 普攻计时器按 SPD 触发     # 普攻 +20 能量
  5. MonsterAI / 玩家输入指令入队
  6. 死亡/胜负判定
```

随机数全部走可复现种子（`seed` 局内固定），PVE 与 PVP 逻辑同源——这是阶段 5A 确定性改造的前置。**战斗随机与局外抽奖随机两套源严格隔离**（见 §11-3）。

### 2.2 伤害公式（DamageCalc.gd，纯函数可单测）

```
伤害 = ATK² / (8 × DEF + ATK)    # 暴击 ×1.5；治疗类 = HP%×MaxHP + 系数×ATK；下限 1
```

TTK 目标（1.8 数值模拟校验）：普通怪 8~15s / 精英 40~70s / BOSS 60~120s。模拟曲线不合预期时调 DEF 系数，不拍脑袋。

### 2.3 状态效果 17 种（BuffSystem）

**持续 15**：流血、中毒、定身、混乱（随机目标 50% 伤害）、减速、破甲、嘲讽、恐惧、潜伏（受击减半）、反震、吸血、护盾（吸收层）、无敌、免死（锁 1 血）、复活。
**即时 2**：斩杀（低于阈值处决）、驱散（移除指定状态）——走技能结算管线不进 buff 生命周期。

每个 buff 字段：`创建时机/触发时机/层数上限/控制抗性豁免`。**控制抗性**：单属性降低被概率施放定身/混乱/恐惧/嘲讽的成功率。

### 2.4 词条钩子（TraitSystem）

**词条总数 44 = 常规 40 + 双刃 4（v1.1 口径修正）**：数值系 12 + 机制系 16 + 流派联动系 12 = 常规 40；双刃独立一档 4 条（燃血/薄甲/狂潮/死线）。六流派（流血/暴击/反伤/控制/能量/召唤），钩子：`on_hit / on_tick / on_kill / on_behit / on_skill`。同流派 ≥3 条触发流派效果（如反伤 3 件 → 反伤翻倍+回血）。

**流派归属（v1.4 补定）**：联动系每流派仅 2 条，不足以凑 3 件触发 → 数值/机制系中语义强相关词条同时归属流派（type 不变，仅 school 字段）：流血 = 撕裂者+放血者+重创；暴击 = 致命韵律+弱点洞悉+精准+裂创；反伤 = 坚壁反震+以伤换伤+荆棘·小；控制 = 霜寒延长+碎冰+寒触+重击+威吓；能量 = 能量导管+超载+聚能+充盈；召唤 = 宠物强化+共鸣+生生不息（持久战叙事）。每流派可凑 3 件，三选一定向Build可达。

**三选一构成（v1.1 精确化）**：槽1 = 数值或机制系随机；槽2 = 流派联动系（85%）或双刃（15%）；槽3 = 全池随机。双刃可选不选（复杂度不作难度）。

### 2.5 MonsterAI 与敌方编成（v1.1 新增定稿——布阵策略的前提）

**敌方编成（让前后排规则真正生效，否则我方 2 单位无布阵可言）：**

| 节点类型 | 编成 | 站位分布 |
|---|---|---|
| 普通 | 3~4 小怪 | 前排 1~2 + 后排 1~2 |
| 精英 | 1 精英 + 2 小怪 | 精英必前排（近战威胁） |
| BOSS | 1 BOSS + 2 小怪 | BOSS 前排，可召唤 +2（敌方上限 6） |
| 深渊/无尽 | 随层数渐增至 6 | 前后排混合 |

我方 2~3 单位（人物+出战宠/替补）对 3~6 敌：近战/远程选择、嘲讽、夺魄打后排、宠物站位均有真实博弈。

**AI 目标选择**：`basic`（最低血）/ `front`（同排最近）/ `back`（优先后排）/ `boss`（技能循环+阶段狂暴）。精英 = 普通怪 + 1 随机怪物侧词条 + 词缀光环特效（程序 hue-shift 变色，不重画）。

### 2.6 路线图与局内延续（v1.1 补跨节点规则）

一局：起点 → 3 层（各 3 选 1 节点）→ BOSS。节点权重：普通 40 / 精英 15 / 事件 15 / 宝箱 12 / 商店 8 / 篝火 10；保底：第 2 层 ≥1 精英、每局 ≥1 事件 ≥1 商店。难度：第 N 层怪全属性 ×(1+0.12N)。

**跨节点延续规则（定稿，篝火/商店/晨星的估值基础）：**
- **HP 跨节点延续**（不回满）；能量清空；buff 全清；词条保留整局
- 篝火：回复 40% HP + 删 1 词条；商店出售治疗药剂（节点间使用，携带上限 3）
- 任意节点战斗失败 = 局结束（结算材料，无惩罚）
- 晨星价值随此规则上升：续航型人物定位成立

### 2.7 探索大地图层（v1.2 新增——自由移动 + 遇敌开战）

**架构**：每层主题（8 主题）一张大地图，人物**自由移动**（虚拟摇杆/点击寻路），怪物作为 NPC 单位分布/游荡在地图上，**走近触发战斗**（接触判定进入 §2 战斗场景）。地图与路线图的关系：路线图仍是局的骨架（选节点），**节点在地图上落地为区域**——进入节点即在对应区域探索移动、遇敌、找出口传送阵。

**技术形态**：
- 地图建议 1536×2048 像素/层（约 2×2.7 屏，比例不限竖屏，可按主题调整）；相机跟随人物
- **三层拼装**：地面层（48×48 tile 平铺，无碰撞）/ 遮挡装饰层（透明散件，**Y-sort**：人物走到树冠后正确被遮挡）/ 氛围层（远山云雾视差）
- **碰撞**：散件在 Godot 侧配 CollisionShape（贴主体轮廓）；人物碰撞箱 32×32（脚部）
- **像素密度基准**（人物适配）：tile 48 / 人物主体 64（画布 96）/ 小散件 96 / 大树建筑 192~288 / BOSS 128
- 地图编成数据进 `maps.json`（每层：tile 主题/散件摆放表/怪物刷新点/传送阵与出口）

**遇敌规则**：
- 怪物在地图上可见（怪物精灵本体，非影子），游荡半径固定；玩家进入其警戒圈（约 120px）怪物追击，接触即开战
- 普通节点区域 3~4 只、精英区 1 精英+2 小怪、BOSS 区 1 BOSS 守传送阵——与 §2.5 编成表一致
- 战斗胜利回地图（怪物消失）；可绕行逃避（绕行是策略）；篝火/商店/宝箱为地图上的交互物件（§1.2 通用散件）
- 换宠在地图上也可操作（战斗外换无 CD）

**素材对应**：提示词总库 §1（tile 24 张 + 主题散件 ~24 件 + 通用散件 5 件 + 氛围 4 件 + 界面背景）；战斗背景 8 主题仍保留（战斗场景用）。

### 2.8 战斗系统素材对应总表（提示词总库 §5/§6/§7/§9.4）

| 战斗表现层 | 提示词 id | 规格 | 触发时机 |
|---|---|---|---|
| 战斗背景 | bg_battle_{forest,glacier,mountain,swamp,ruin,volcano,dungeon,abyss} ×8 | 480×800 | 按遇敌区域主题切换 |
| 技能图标 | sk_{zs,ck,fs,fz}_{拼音} ×20 | 64×64 | 5 技能条按钮（能量/CD 状态用程序遮罩） |
| 技能条底板 | ui_panel_battle | 256×64 九宫格 | 战斗 HUD 底部横条 |
| P0 技能特效 | fx_{zs_lieshan,...} ×8 | 240×240 帧序列 | 首批 8 个技能命中 |
| 通用特效 | fx_slash / fx_hit / fx_crit / fx_heal / fx_shield ×5 | 240×240 帧序列 | 未做专效的技能兜底 |
| P1 技能特效 | fx_{其余12技能} ×12 | 240×240 帧序列 | 补齐 20 技能 |
| buff 图标 | buff_{流血,中毒,定身,...} ×17 | 32×32 | 单位头顶/血条角标 |
| buff 表现 | sfx_{bleed,poison,stun,...} ×8 | 128×128 帧序列 | buff 生效时的附着表现 |

**特效分级兜底链**：专效（fx_{技能id}）→ 通用（fx_slash 等按伤害类型路由）→ 程序闪光（ColorRect 闪烁）。任一技能没出图也能打，出图后热替换——美术不阻塞战斗开发。

---

## 3. 宠物系统（P1，抽奖的主出口）

### 3.1 首批 8 只（pets.json）

| id | 名字 | 稀有度 | 定位 | 参战技能（1~2 个） |
|---|---|---|---|---|
| pet_frostwolf | 霜狼 | 蓝 | 前排坦克 | 撕咬(k1.2)、寒嚎(减速15%×3s) |
| pet_foxfire | 火狐 | 蓝 | 远程输出 | 火球(k1.5)、灼烧(流血) |
| pet_rockturtle | 岩龟 | 白 | 防御嘲讽 | 冲撞(k0.8+击退一排) |
| pet_thunderhawk | 雷隼 | 白 | 高速输出 | 穿啄(k1.0，SPD×1.5) |
| pet_eyescat | 幽瞳猫 | 紫 | 控制辅助 | 惊惧(恐惧1.5s)、护主(嘲讽2s) |
| pet_holydeer | 圣鹿 | 紫 | 治疗辅助 | 鹿愈(回复8%MaxHP) |
| pet_emberling | 幼龙·烬 | 金 | 群伤 | 龙息(k0.9 全体)、余烬(灼烧叠加) |
| pet_shadowviper | 双头蛇·影 | 金 | 毒控 | 毒牙(中毒×3层)、缠绕(定身1.2s) |

**宠物属性 4 项（v1.1 修正：删"能量"，与 CD 自动施放冲突）**：HP/ATK/DEF/SPD。战斗内宠物 = 全自动普攻 + 技能 CD 转好自动释放（不吃 5 技能条、不吃能量）。帧规格：每只 13 帧（待机4/攻击4/受击2/死亡3）+ 头像 1 + 进化形态 ×1。

### 3.2 培养链路（六环节全 JSON 化）

```
获得(抽奖/远征掉落) → 升级(宠物经验：吞噬低级宠或宠物粮)
→ 突破(同名突破材料，每层 10/15/20/25/30，上限 5 层，属性 +8%/层) → 进化(1次，外形+技能强化)
→ 技能学习(宠物技能书：抽奖产出，可遗忘返还50%) → 参战(出战1+替补1，60s换宠CD+免死1次)
```

- **资质**：1~5 星（获得时随机），只影响成长系数（资质星数×成长率 = 每级属性增量），可用"资质果"重随。
- **重复宠物**：抽奖/掉落重复 → 自动转化为该宠专属突破材料 ×20（金宠 ×40）——配合突破消耗 10~30/层，重复 2~4 只可点满一层（**绝不浪费**）。
- **吞噬**：白/蓝宠喂给任意宠转经验，金宠禁吞。

### 3.3 pets.json schema

```json
[ {"id":"pet_frostwolf","name":"霜狼","rarity":"blue","role":"tank",
   "base":{"hp":60,"atk":14,"def":6,"spd":1.1},
   "growth":{"hp":8,"atk":2.5,"def":1.2},
   "star_cap":5,"evolve_to":"pet_frostwolf_evo",
   "skills":[{"id":"pw_bite","k":1.2,"cd":6},{"id":"pw_howl","type":"slow","pct":0.15,"dur":3,"cd":9}]} ]
```

### 3.4 素材对应（宠物，提示词总库 §3 + §13.3 物品）

| 素材 | 提示词 id | 规格 | 用途 |
|---|---|---|---|
| 宠物精灵 | pet_{frostwolf,foxfire,rockturtle,thunderhawk,eyescat,holydeer,emberling,shadowviper} ×8 | 64×64，13 帧（待机4/攻击4/受击2/死亡3） | 地图跟随+战斗参战 |
| 进化形态 | {同上}_evo ×8 | 64×64，13 帧 | 进化后换装（同名覆盖） |
| 宠物头像 | 头像 1/只（母图裁切） | 96×96 | 出战栏/替补栏/图鉴 |
| 培养物品 | itm_pet_food / itm_aptitude_fruit / itm_pet_book / itm_evolve_crystal / itm_break_crystal / itm_gold_shard | 48×48 | 升级/资质/技能/进化/突破/金宠合成 |
| 换宠 UI | ui_panel_battle 上程序摆头像 | — | 战斗内换宠按钮 |

**程序化策略**：进化形态与基础形态同构图（提示词已注明"同构图强化形态"），帧数一致可直接替换；宠物不吃能量（§3.1），战斗内无需专属 HUD。

---

## 4. 抽奖系统（新增模块，灵魂石/金币的出口）

### 4.1 池子设计（双池，无人物池）

| 池 | 消耗 | 产出 | 入口 |
|---|---|---|---|
| **宠物寻宝**（主池） | 灵魂石 ×10/抽 | 宠物整只、宠物技能书、资质果、宠物粮 | 主界面"祭坛" |
| **装备秘库**（副池） | 金币 ×5000/抽 | 随机部位装备（武器/甲/饰）、强化石、宝石 | 主界面"秘库" |

### 4.2 概率与保底（全公示，保底计数跨池独立、永久累计不清零）

| 稀有度 | 宠物池 | 装备池 |
|---|---|---|
| 白 | 55% | 50% |
| 蓝 | 28% | 30% |
| 紫 | 14% | 16% |
| 金 | 3% | 4% |

- 宠物池：**20 抽保底紫、60 抽保底金**（必出整只）；装备池：**15 抽保底紫、50 抽保底金**。
- **十连**：9 折（灵魂石×90 / 金币×45000）+ 必出 ≥1 紫。
- **每日免费 1 抽**（宠物池，零点刷新）；连续 7 天免费抽 → 灵魂石 ×50（v1.1 修正：签到活动第 7 天已送十连券，避免双大奖重复）。
- 出金概率活动期可上浮（+50%），**保底计数与保底线永不上调**。
- 抽奖历史本地记录 100 条（时间/池/结果），可自查。

### 4.3 重复与兜底（低挫败落地）

- 重复宠物 → 该宠突破材料 ×20；重复金宠 → ×40。
- 重复装备 → 自动分解为强化石（按稀有度 5/10/20/40）。
- 保底计数 UI 常显（"距金保底还剩 N 抽"）。

### 4.4 gacha.json schema（程序侧随机也走种子，可单测分布）

```json
{ "pools":[
  {"id":"pet","name":"宠物寻宝","currency":"soul","cost":10,"cost_ten":90,
   "rates":{"white":0.55,"blue":0.28,"purple":0.14,"gold":0.03},
   "pity":{"purple":20,"gold":60},"daily_free":1,
   "slots":[ {"type":"pet","weight":30}, {"type":"pet_skill_book","weight":15},
             {"type":"star_fruit","weight":10}, {"type":"pet_food","weight":20} ]},
  {"id":"equip","name":"装备秘库","currency":"gold","cost":5000,"cost_ten":45000,
   "rates":{"white":0.50,"blue":0.30,"purple":0.16,"gold":0.04},
   "pity":{"purple":15,"gold":50} } ] }
```

存档新增：`"gacha":{"pet":{"count":0,"pity_gold":0,"pity_purple":0,"last_free":""},"equip":{...},"history":[]}`。

### 4.5 UI（全部走 G.gd 工厂 + Godot Control 绘制）

- 抽奖大厅：双池 Tab + 概率公示页（羊皮纸面板 + 表格）。
- 抽取动效：卡背翻转（金=金光粒子、紫=紫光，2D 帧动画约 15 帧，程序可先占位）。
- 结果页：稀有度边框复用词条三选一的四档边框（白/蓝/紫/金，**一套素材三处用**：三选一/抽奖/图鉴）。

### 4.6 抽奖玩法逻辑链（输入→处理→输出）

```
输入: 玩家点击"单抽/十连" → 校验(货币够不够/每日免费次数/保底计数读取)
处理: gacha.json rates 按稀有度滚动(局外RNG可存档) → 命中池 slots 权重 → 结果实体化
      保底插值: pity_purple/pity_gold 到点强制提档; 十连附加"必出≥1紫"
输出: 实体发放(宠物→图鉴+重复转化突破材料 / 装备→背包+重复分解强化石 / 材料→背包)
      保底计数+1并常显 → 抽奖历史入栈(100条) → 动效(fx_gacha_flip/burst) → 结果卡(稀有度框)
```

### 4.7 素材对应（抽奖，提示词总库 §10）

| 素材 | 提示词 id | 规格 | 用途 |
|---|---|---|---|
| 主池入口 | icon_altar（祭坛·灵魂石） | 64×64 | 主界面"祭坛"按钮 |
| 副池入口 | icon_vault（秘库·金币） | 64×64 | 主界面"秘库"按钮 |
| 卡背 | gacha_card_{white,blue,purple,gold} ×4 | 96×128 | 翻转前的卡面 |
| 翻转动效 | fx_gacha_flip | 12 帧序列 | 单抽揭示 |
| 十连爆发 | fx_gacha_burst | 10 帧序列 | 十连结算金光 |
| 概率公示底板 | ui_panel_odds | 480×800 九宫格 | 概率表格页底 |
| 结果卡框 | frame_* ×4（§9.1 与词条共用） | 96×128 | 抽奖结果/三选一/图鉴三处复用 |
| 十连券 | itm_ticket_ten | 48×48 | 签到第 7 天大奖 |

**共用复用声明**：frame_* 一套 4 张是词条三选一（§2.4）、抽奖结果（本节）、图鉴（P2）三处共用——总量按 4 张计，勿重复生成。

---

## 5. 活动系统（配置驱动框架 v0 → 运营增量发布）

### 5.1 框架（act/ActCenter.gd）

- 三级结构：主界面"活动"入口（红点）→ 活动列表 → 单活动页。
- 活动配置**服务端下发**（id/类型/起止/标题/图/奖励项），未知类型安全降级隐藏；GM 后台改配置即上线，不发包。
- 领奖状态存本地 `act_progress`，随云存档同步。
- 本地内置 `acts_default.json` 兜底（断网可玩）。

### 5.2 首发两个模板（验证链路）

**登录签到**：7 日循环，第 7 天大奖（十连券/金宠碎片）；补签用远征币 ×200/天，最多补 2 天。
**限时兑换**：远征币/荣誉兑换材料（宠物粮/资质果/强化石/宝石），每周限购量轮换。

### 5.3 后续模板（仅预留类型字段，不做实现）

节日登录礼 / 累计充值（P2 再评估）/ 限时副本（配合当周词条轮换 50%）/ PVP 活动赛。

### 5.4 红点规则

`有可领奖 or 有新活动 or 免费抽未用` → 主界面入口红点；红点逻辑集中 `ActCenter.has_red_dot()` 一处判断。

### 5.5 素材对应（活动，提示词总库 §11）

| 素材 | 提示词 id | 规格 | 用途 |
|---|---|---|---|
| 签到日历框 | ui_signin | 448×288 | 7 日签到 7 格摆位 |
| 兑换列表框 | ui_exchange | 448×480 | 限时兑换条目摆位 |
| 公告框 | ui_notice | 448×320 | 公告滚动文字容器 |
| 活动横幅 | ui_banner_event | 480×160 | 主界面活动入口头图 |
| 抽奖横幅 | ui_banner_gacha | 480×160 | 抽奖活动入口头图 |
| 兑换奖池物品 | itm_* / gem_*（§13 复用） | 48×48 | 兑换列表内嵌图标 |
| 补签消耗 | cur_expedition（远征币 ×200/天） | 32×32 | 补签按钮角标 |

**框架约束**：活动配置服务端下发含 `banner_img` 字段时优先用下发图，缺省回落 `ui_banner_event` + 程序标题文字——运营无图也能发活动。

---

## 6. 局外养成 6 线 + 货币产消总表

6 线：等级+天赋树（每5级1点，三系×10节点）/ 装备（强化+精炼+宝石+外观3阶）/ 宠物（§3）/ 技能书（20技能入池，每级k+5%上限10级）/ 坐骑（6类升阶）/ 称号成就。

**装备规则（v1.1 新增定稿）**：
- **武器 4 系绑定人物**（剑→破军 / 枪→穿杨 / 杖→霜语 / 锤→晨星），大剑不能给霜语装；**甲/饰全人物通用**——养成全局共用但武器不串用，四系武器各自有强化/精炼线
- 获取渠道：装备秘库（主）+ BOSS 掉落 + 商店（v1.1 补：不能只有抽奖单渠道）
- 强化（每级+10%，成功率 `0.9^级`，失败不掉级）+ 精炼（4词条洗练，锁定消耗）+ 宝石（3色×5级）
- 外观升阶（ATK 50/120/250）仅武器+甲两部件

**货币 4 种产消闭环**：

| 货币 | 主要产出 | 主要消耗 |
|---|---|---|
| 金币 | 远征结算/扫荡/装备秘库抽 | 装备秘库、强化、商店 |
| 远征币 | 词条三选一保留/通关/签到 | 技能书、精炼、补签 |
| 灵魂石 | 精英击杀/深渊/PVP 荣誉兑换 | 宠物寻宝、宠物突破 |
| 荣誉 | PVP 结算/成就 | 称号、限时兑换 |

**扫荡**：已通关层一键扫荡 70% 收益。**连续失败 3 次保底礼包**：灵魂石×30+宠物粮×5。

### 6.1 养成 6 线素材对应（提示词总库 §13）

| 养成线 | 面板底板 | 功能图标 | 备注 |
|---|---|---|---|
| 等级+天赋树 | ui_panel_talent | — | 节点连线程序绘制（星点背景自带） |
| 装备 | ui_panel_equip | slot_{sword,spear,staff,hammer,armor,accessory} ×6 | 4 武器槽分系+甲/饰通用（§6 武器绑人物） |
| 宝石镶嵌 | （并入 ui_panel_equip） | gem_{atk,def,hp}_{1..5} ×15 | 3 色×5 级，同色递增可一眼分辨 |
| 精炼 | ui_panel_refine | itm_refine_stone + itm_lock_rune | 洗练+锁定消耗 |
| 宠物 | （§3.4） | 见 §3.4 培养物品 6 项 | — |
| 技能书 | ui_panel_skillbook | itm_pet_book（宠物技能书）/ 技能升级走 sk_* 图标 | 20 技能入池 |
| 坐骑 | ui_panel_mount | mount_* ×12 + fx_mount_summon | 6 类×2 阶，§15.1 |
| 称号成就 | ui_panel_title | cur_honor（兑换货币） | 称号本体程序文字渲染 |

**货币图标**（顶栏常驻，§13.2）：cur_gold / cur_expedition / cur_soul / cur_honor ×4，32×32——产消总表四货币的 UI 出口。

**通用物品**（§13.3 ×24）：itm_* 覆盖背包/消耗/材料/药剂/兑换奖池；装备强化线消耗 itm_enhance_stone，BOSS 掉落材料 itm_{iron_ore,mithril,dragon_scale,magic_crystal}。

---

## 7. PVP 实时对战（完整设计，v1.3 扩充）

### 7.1 模式矩阵

| 模式 | 上阵 | 属性规则 | 词条/宝石 | 匹配 | 结算 |
|---|---|---|---|---|---|
| 排位 1v1 | 1 人+1 宠/方 | **白板**（基准属性表，与等级无关） | 不生效 | ELO ±100 段内 | ELO ±25/−20，赛季段位 |
| 排位 2v2 | 2 人+各 1 宠/方 | 白板 | 不生效 | 队伍均分 ELO | 同上（队伍共享） |
| 排位 3v3 | 3 人+各 1 宠/方 | 白板 | 不生效 | 同上 | 同上 |
| 休闲 1v1~3v3 | 同上 | **战力带入**（等级/装备/宠物/技能书全生效） | 不生效 | 无段位约束，快速匹配 | 荣誉少量、无 ELO |

**白板化细则（排位）**：天赋/装备数值/宠物数值/技能书 k 加成全部失效，仅保留外观差异（皮肤/装备 3 阶外观/坐骑）→ 排位 = **人物选择 + 宠物选择 + 阵容站位 + 技能操作**，纯技巧对局。
**休闲补偿**：属性差超 20% 时低方补 10% 全属性；120s 超时按剩余血量 %（**溢出治疗不计入**，防治疗拖延流）。

### 7.2 对战流程状态机（匹配 → 结算）

```
LOBBY(PVP大厅,ui_panel_pvp_lobby)
  → 选模式(1v1/2v2/3v3 + 排位/休闲) → MATCHING(匹配中,ui_panel_pvp_match)
      2v2/3v3: 先组队(邀请/自动补位,ui_panel_pvp_team) 再全队入匹配
  → MATCHED: 服务端建房间,下发 seed(战斗种子)+双方阵容快照
  → DEPLOY(阵型): 30s 内拖动站位(列选择;宠物位固定随人物) → 锁定
  → BATTLE: 锁步帧同步对战(§7.3), 战斗内核=§2 同源(PVE/PVP 一套逻辑)
  → SETTLE: 服务端权威判胜负 → ELO/荣誉结算 → 战绩页 → LOBBY
  掉线: 30s 内可重连; 超时判负(排位扣分减半,休闲不扣)
```

### 7.3 锁步帧同步（确定性，PVE 战斗内核直接复用）

**原理**：双方各自本地跑同一份确定性战斗模拟，每帧只交换**操作指令**（几十字节），不交换任何游戏状态——带宽极小且天然防作弊。

```
逻辑帧(固定 15fps 步进, 与渲染帧解耦):
  1. 双方客户端把本帧输入(技能i/换宠/托管)编码为指令 {tick, unit, action}
  2. 上传 pvp_relay(自建轻量房间中继, wss)
  3. relay 收齐双方本帧指令 → 广播"确认帧"给双方
  4. 客户端收到确认帧才推进 tick: 指令入队 → §2.1 同一战斗时序
渲染帧(60fps): 插值平滑表现; 指令本地乐观显示(前摇掩盖确认延迟)
```

**确定性三铁律**（PVE 阶段 1 就按此写，5A 改造成本归零）：
1. 所有随机走局内种子（房间 seed，双方一致）
2. 禁浮点比较/字典遍历序（伤害公式全整数运算或固定精度）
3. tick 固定步长，渲染与逻辑解耦

**掉线重连**：客户端重连后向 relay 拉取房间快照 seed + 已确认指令日志，本地重放至当前帧——确定性保证重放结果与对端一致。

### 7.4 对战内规则差异（vs PVE，仅 3 处）

| 差异点 | PVE | PVP |
|---|---|---|
| 指令来源 | MonsterAI（§2.5） | 对端玩家指令（relay 转发） |
| 站位显示 | 紧凑高亮 | 1v1 紧凑 / 2v2、3v3 全场 20 站位展开 |
| 超时 | 无 | 120s 按剩余血量%（溢出治疗不计） |

布阵规则（§1.3）前后排/近战远程限制**完全同源**——PVE 锻炼的站位理解直接迁移 PVP，这是设计意图。

### 7.5 ELO 与段位赛季

- ELO：胜 +25 / 负 −20；组队按队伍均分匹配；±100 分段内搜索，超 30s 放宽 ±200
- 段位 5 档：青铜→白银→黄金→铂金→钻石（rank_* ×5 徽章）；4 周赛季
- 赛季结算：按段位发荣誉+限定称号；超期未打降一档（服务端权威）
- 赛季榜（ui_panel_season）：本地榜 + 好友榜（阶段 6 社交后）

### 7.6 PVP 素材对应（提示词总库 §12）

| 素材 | 提示词 id | 规格 | 用途 |
|---|---|---|---|
| 段位徽章 | rank_{bronze,silver,gold,platinum,diamond} ×5 | 64×64 | 资料页/赛季榜/结算页 |
| PVP 大厅底板 | ui_panel_pvp_lobby | 256×256 九宫格 | 模式选择页 |
| 匹配面板底板 | ui_panel_pvp_match | 256×256 九宫格 | 匹配倒计时/双方信息 |
| 组队房间底板 | ui_panel_pvp_team | 256×256 九宫格 | 3 队员头像位 |
| 赛季榜底板 | ui_panel_season | 480×800 | 榜单整页 |
| 战斗复用 | bg_battle_* / sk_* / fx_* / buff_* / 角色宠物怪物精灵 | — | **零新增**，战斗表现层全复用 PVE 素材 |

**PVP 素材增量极小（9 张）**——战斗内核与表现层复用是本架构的最大红利；这正是"先单机后 PVP"成本可控的根源。

### 7.7 PVP 开发渐进（阶段 5 内部排序，5A→5E）

```
5A 地基: 账号服务(注册/登录/令牌) + 确定性改造收口(种子/整数化/固定步进)+单测
   + pvp_relay v0(wss 房间中继/掉线判负) + FrameSync + 联调工具(双开模拟器)
   验收: 双设备同 seed 同指令日志 → 战斗逐帧一致(哈希比对)
5B 1v1 竖切: 匹配→阵型→对战→ELO→战绩 闭环(只用已有战斗内核, 显示紧凑模式)
   验收: 双真机 10 局稳定; 前摇延迟掩埋手感可接受; 掉线判负正确
5C 2v2: 组队(邀请/自动补位) + 全场站位显示 + 队伍共享 ELO
5D 3v3: 六人同房间(压力测试: relay 并发/指令风暴) + 全场显示优化
5E 赛季: 段位/赛季榜/结算奖励/降级; 排位白板化实测(晨星治疗 vs 血量%结算)
   验收: 3v3 六人 10 局无掉线; 积分双端一致; 排位归一化生效
```

---

## 8. 图片需求清单（v1.3：直接对应提示词总库 v2.1）

> 生成用提示词单一事实来源：[2026-09-15-图片提示词总库.md](2026-09-15-图片提示词总库.md) **v2.1（343 条，16 节）**——每条提示词自带游戏背景/风格/规格/内容/禁止项，整段复制即用。素材明细规格见 [2026-09-15-美术素材总清单.md](2026-09-15-美术素材总清单.md)。风格锚点：3/4 俯视角 2D 像素、2.5 头身、深棕硬边轮廓、24~32 色、暖金底+暗红/青蓝高亮、透明背景、**无文字**。

| 优先级 | 对应提示词节 | 条目数 | 关键项 |
|---|---|---|---|
| P0 | §1 地图 / §2 角色 / §5.1 特效 / §6 状态 / §7 技能图标 / §4.1 怪首批 / §9.4 战斗面板 | ~128 | 角色 4 母图+姿势+部件、技能特效 8+通用 5、buff 17+特效 8、怪 6+BOSS 5、战斗背景 8+节点 8、tile 首主题+散件、ui_panel_battle、sk_* ×20、NPC 3 |
| P1 | §3 宠物 / §4.2 怪全量 / §5.2~5.3 / §8 节点 / §9.1~9.3 框件 / §10 抽奖 / §11 活动 / §12 PVP / §13 养成 | ~137 | 宠物 16、怪全量 31、特效补 12、节点 8、稀有度框 4+流派 6、抽奖 9、活动 5、PVP 9、养成 UI 55 |
| P2 | §14 NPC 扩展 / §15 坐骑与发行 / 图鉴（复用 pet_*/mon_* 母图） | ~72 | 坐骑 18、发行 5（含 icon_app）、图鉴 30、NPC 全量 7 |

> 口径说明：v6 原估"P0 65~75 张"实为大件母图口径（漏计小图标与逐张口径），以素材总清单明细为准；v2.1 提示词总库 343 条已全覆盖三档，含应用图标等发行物料。

---

## 9. 数据表总清单（data/，14 张）

`roles.json`✅ / `skills.json` / `traits.json` / `monsters.json` / `nodes.json` / `combos.json` / `growth.json` / `talents.json` / `pets.json` / `mounts.json` / `achievements.json` / `npcs.json` / **`gacha.json`（新增）** / **`maps.json`（v1.2 新增：层地图 tile/散件/怪物/传送配置）** + `acts_default.json`（活动兜底）。全部走 DataManager 统一加载 + 启动校验（缺表/字段错 push_error）。

---

## 10. 开发路径（v1.3 重排：先单机完整闭环，后 PVP 按模式渐进）

> **总原则（§0.2）**：阶段 0~4 = 单机完整闭环（可独立上线测试）；阶段 5 = PVP 渐进 5A→5E（§7.7）；阶段 6~7 = 社交与发布。每阶段首行**素材依赖**标注"该阶段开发所需的提示词总库条目"——美术按此提前排产，程序用兜底链（§2.8）不被美术阻塞。

### 阶段 0 · 工程奠基
- [ ] 0.1 git init + 备份推私有仓
- [ ] 0.2 竖屏 480×800 + 像素完美 + autoload 骨架（**已建**，G.gd/roles.json/Main.tscn 在位）
- [ ] 0.3 复用素材/脚本验证（.pwd/.aef 播放）
- [ ] 0.4 data/ 14 张表骨架 + DataManager（含 maps.json / gacha.json）
- **素材依赖**：无（纯工程）；可同步生成 `mkt_logo`+`bg_login` 搭建登录页

### 阶段 1 · 战斗竖切（单机核心）
- [ ] 1.1 DamageCalc 公式+单测（3 组手算断言）
- [ ] 1.2 Combatant 状态机 + 20 站位 + 前后排规则（按 §1.3 定稿版；**按确定性三铁律写**，§7.3）
- [ ] 1.3 BuffSystem 17 状态生命周期
- [ ] 1.4 SkillSystem 20 技能+能量+前摇+连携
- [ ] 1.5 战斗 UI：5 技能条+换宠+托管+飘字+HP条
- [ ] 1.6 MonsterAI + 敌方编成（§2.5 表）+ 5 怪 + 1 BOSS + 精英词条
- [ ] 1.7 结算浮层→重开
- [ ] 1.8 TTK 模拟（纯脚本）：1/10/30/50 级表 + **分人物能量经济（霜语双重惩罚验证）** + **晨星治疗曲线 vs 怪物 ×(1+0.12N) 缩放** + **深渊 25 层终层（×4）vs lv60 满养成可行性**
- **素材依赖（提示词 §2.2/§4.1/§5/§6/§7/§9.4/§1.5）**：role_* 姿势帧 ×12、mon 首批 6+BOSS 1、fx 通用 5+P0 技能 8、buff_* 17+sfx 8、sk_* 20、ui_panel_battle、bg_battle_{forest} ×1
- 验收：4人物×5技能全可用；站位有策略影响；TTK 8~15s；能量经济分人物无 deadlock（无人物出现"凑不齐能量放关键技"）

### 阶段 2 · 远征循环 + 探索大地图（单机骨架）
- [ ] 2.1 RouteGenerator 权重/保底+单测（100 局分布）
- [ ] 2.2 路线图 UI + 节点进入 + **跨节点延续（HP延续/能量清空/buff清空，§2.6）**
- [ ] 2.3 **探索大地图（v1.2）**：TileMap 地面层 + Y-sort 散件遮挡 + 碰撞 + 虚拟摇杆移动 + 怪物游荡/警戒/追击/接触开战 + 传送阵节点出口 + maps.json
- [ ] 2.4 TraitSystem 44 词条+六流派+流派效果
- [ ] 2.5 三选一 UI（v1.1 槽位构成）+ 四档稀有度边框（与抽奖共用）
- [ ] 2.6 事件/宝箱/商店（含治疗药剂）/篝火（40%HP+删词条）+ 精英
- [ ] 2.7 局结算+材料+存档
- [ ] 2.8 NPC 系统 + 首批 3 人挂载
- **素材依赖（提示词 §1.1~1.4/§8/§9.1~9.3/§14）**：tile_{forest} ×3 基础、deco_{forest,common}、node_* ×8、ui_panel_card、frame_* ×4、school_* ×6、icon_double_edge、npc_* ×3、{人物id}_pose_walk 移动帧
- 验收（生死线）：连打 5 局不腻；流派走向分化；combo 有爽感；篝火/药剂使用率 >0（延续规则真实起作用）；**地图移动手感/绕怪策略实机认可**

### 阶段 3 · 养成 6 线 + 抽奖（单机经济闭环）
- [ ] 3.1 等级/天赋树（三系×10）
- [ ] 3.2 装备强化+精炼+宝石 + **武器 4 系绑人物（§6）**
- [ ] 3.3 宠物完整线（资质/成长/突破量化/进化/吞噬/技能/参战+替补换宠）
- [ ] 3.4 技能书 + 坐骑 + 称号
- [ ] 3.5 4 货币钱包 + 产消接口
- [ ] 3.6 抽奖系统：gacha.json + 双池 + 保底/重复转化 + 概率公示页 + 每日免费抽 + 抽取动效（先占位）+ 分布单测（10000 抽卡方检验）
- [ ] 3.7 本地存档版本化 + 导出/导入
- **素材依赖（提示词 §3/§10/§11/§13）**：pet_* ×16（+进化 8）、gem_* ×15、cur_* ×4、itm_* ×24、slot_* ×6、ui_panel_{talent,equip,refine,title,skillbook} ×5、gacha 9 张（卡背/动效/入口/公示底板）、ui_signin/exchange/notice、ui_banner_gacha
- 验收：卡关→刷局外→消解；抽奖 10000 抽模拟分布与公示误差 <1%；保底触发正确；换宠在 BOSS 局有实际使用场景

### 阶段 4 · 单机深化（**单机完整闭环终点**）
- [ ] 4.1 深渊 25 层 + 无尽模式（8 主题全开，难度 ×(1+0.12N) 至 ×4）
- [ ] 4.2 扫荡（70% 收益）+ 连续失败 3 次保底礼包
- [ ] 4.3 每周词条轮换（当周 50% 词条池替换，服务端/本地配置）
- [ ] 4.4 活动 v0 全链路：签到 7 日 + 限时兑换（GM 配置发布，§5）
- [ ] 4.5 坐骑系统（6 类×2 阶 + 出场特效，提示词 §15.1/15.2）
- [ ] 4.6 图鉴 v0（宠物/怪物/装备三册，复用母图+frame_*）
- **素材依赖（提示词 §1.1~1.2 剩余主题/§4.2/§13.4/§15.1/§15.2）**：tile 剩余 7 主题 ×3 基础、deco 剩余 7 主题、mon 全量 24、ui_panel_mount、mount_* ×12+fx_mount_summon、bg_battle 全 8
- 验收（单机上线门槛）：8 主题全可玩；深渊 25 层通关曲线合理（终层 TTK 模拟通过）；签到/兑换线上可发可下线；**单机版本可交 20 人友测**

### 阶段 5 · 实时 PVP（按模式渐进，详见 §7.7）
- [ ] 5A 地基：账号服务 + 确定性改造收口（种子/整数化/固定步进）+ 单测 + pvp_relay v0 + FrameSync + 双开联调工具
- [ ] 5B 1v1 竖切：匹配→阵型→对战→ELO→战绩闭环
- [ ] 5C 2v2：组队 + 全场站位显示 + 队伍共享 ELO
- [ ] 5D 3v3：六人房间压力测试 + 显示优化
- [ ] 5E 赛季：段位/赛季榜/奖励/降级 + 排位白板化实测（晨星治疗 vs 120s 血量%）
- **素材依赖（提示词 §12，仅 9 张）**：rank_* ×5、ui_panel_{pvp_lobby,pvp_match,pvp_team,season} ×4——战斗表现层零新增（§7.6）
- 验收：双设备稳定；3v3 六人 10 局无掉线；积分一致；排位归一化生效

### 阶段 6 · 社交
- [ ] 6.1 云存档+冲突合并
- [ ] 6.2 世界聊天+好友+三榜
- [ ] 6.3 异步组队（镜像 AI）
- **素材依赖**：聊天/好友 UI 全程序绘制（G.gd 工厂）；赛季榜好友位复用 ui_panel_season
- 验收：双设备互见互聊，榜单位次一致

### 阶段 7 · 打磨发布
- [ ] 7.1 热更+断线重连
- [ ] 7.2 音效/BGM/震动
- [ ] 7.3 隐私政策/商店素材
- [ ] 7.4 商店截图 + 宣传物料
- **素材依赖（提示词 §15.3）**：icon_app ×1（512×512 应用图标）、mkt_{logo,splash,loading,store} ×4；商店截图 = 运行实机截屏 + mkt_store 头图
- 验收：20 人友测无阻断；商店初审过；测试活动可线上发布/下线

---

## 11. 决策清单（改任一条需回头再审；1~5 为 v1.0，6~11 为 v1.1，12 为 v1.2，13~14 为 v1.3 新增）

1. **抽奖不抽人物**：人物固定 4 名无池；抽奖 = 宠物池（灵魂石）+ 装备池（金币）
2. **保底永久累计**：跨池独立、不清零、不上调；重复全转化材料
3. 抽奖随机走局外 RNG（可存档），**PVE/PVP 战斗随机仍走局内种子**——两套随机源严格隔离
4. 四档稀有度边框一套素材三处复用：词条三选一 / 抽奖结果 / 未来图鉴
5. 活动只做框架+2 模板，一切具体活动线上配置增量发布
6. **普攻规则定稿**：近战限前排；远程任意排默认最低血；指定排是技能特权（夺魄=后排+无视50%DEF）
7. **敌方编成定稿**（§2.5 表）：普通 3~4 / 精英 1+2 / BOSS 1+2+召唤；布阵策略的前提
8. **局内延续定稿**：HP 跨节点延续、能量/buff 清空、词条保留；篝火 40%HP
9. **排位白板化**：等级/天赋/装备数值/宠物数值/技能书加成全不生效，仅选择与操作
10. **武器 4 系绑人物**，甲/饰通用；装备获取三渠道（秘库/BOSS/商店）
11. **词条口径 44 = 40 常规 + 4 双刃**；三选一槽2 = 流派 85% / 双刃 15%
12. **探索大地图层（v1.2）**：自由移动 + 遇敌开战；路线图仍是局骨架，节点落地为地图区域；素材 = tile+散件分层拼装（比例不限竖屏），战斗仍走 §2 站位制；数据进 maps.json
13. **开发顺序定稿（v1.3）**：先单机完整闭环（阶段 0~4：奠基→战斗→远征循环→养成+抽奖→单机深化，可独立上线友测），PVP 后置且按模式渐进（5A 地基→5B 1v1→5C 2v2→5D 3v3→5E 赛季）；每阶段绑定素材依赖，程序走特效兜底链不被美术阻塞
14. **玩法↔素材单一对应（v1.3）**：所有玩法模块的素材需求以 §0.4 总表 + 各系统"素材对应"小节为准，id 与提示词总库 v2.1 严格一致；新增素材必须先入总库再开发引用，禁止临时拍 id
15. **近战推进与流派归属（v1.4，第一轮内容制作实测修正）**：① 近战在敌方前排全灭时可攻击任意存活单位（防普通节点后排怪僵局/必败）；② DOT 每秒伤害按 tick 浮点累积、≥1 才整数结算（防小额毒被每 tick 取整下限放大 30 倍）；③ 流派触发词条数从"联动系 12 条"扩为"联动+语义归属"（§2.4 表），否则 ≥3 件在数据上不可达成

## 12. 风险清单

| 风险 | 对策 |
|---|---|
| 抽奖概率被质疑 | 概率公示+10000 抽分布单测+本地历史可查 |
| 灵魂石产消失衡 | 阶段 3 结束跑 7 日产消模拟表（每日预期获得 vs 抽奖需求） |
| 保底计数与活动上浮互踩 | 上浮只调 rates，pity 字段服务端下发只读 |
| 霜语能量双重惩罚（SPD 0.9+费用最高） | 1.8 分人物模拟，不达预期调 SPD 或费用 |
| 晨星治疗线性 vs 怪物指数缩放 | 1.8 治疗曲线模拟；不达预期给治疗 HP% 系数随层微涨 |
| 深渊 ×4 缩放 vs lv60 养成上限 | 1.8 模拟终层 TTK；靠词条/流派补深度而非数值 |
| PVP 治疗拖延 | 溢出治疗不计入剩余血量%；5E 排位实测 |
| AI 重绘风格漂移 | 3 张批准锚点 + "保持不变只改一个目标"迭代 + 校色脚本 |
| 锁步帧同步超期 | 降级服务端权威；5B 只上 1v1，2v2/3v3 顺延不阻塞上线 |
| 单机阶段素材不齐阻塞开发 | 特效分级兜底链（§2.8）+ 占位色块先行 + 素材热替换；阶段素材依赖表倒排美术排产 |

## 13. 逻辑自查记录（2026-09-15，v1.0→v1.1 修正依据）

**A 类：硬矛盾/缺口（已全部修正进正文，不修必返工）**
1. 攻击规则自相矛盾：v6"远程可打后排"使夺魄"无视前排"附加值=0 → §1.3 重定稿
2. 敌方编成未定义：我方 2 单位无布阵策略可言 → §2.5 编成表
3. 局内跨节点延续规则缺失：篝火/商店/晨星估值悬空 → §2.6 定稿
4. PVP 排位"完全公平"范围含糊（等级/天赋/技能书/宠物基准均未明确）→ §7 白板规则
5. 武器类型 vs 养成全局共用矛盾（大剑给霜语？）→ §6 武器 4 系绑人物
6. 词条总数口径：12+16+12=40 已满，双刃 4 条无归属 → §2.4 总 44 口径
7. 宠物"能量"属性与 CD 自动施放冲突 → §3.1 删能量，4 属性

**B 类：数值/体验风险（不阻塞开工，并入 1.8 模拟与 4.6 实测）**
8. 霜语 SPD 0.9 + 技能费用最高 = 能量回充双重惩罚 → 分人物能量经济模拟
9. 晨星治疗线性成长 vs 怪物 ×(1+0.12N) 缩放 → 治疗曲线并入 TTK 模拟
10. PVP 120s 血量%结算 + 治疗拖延 → 溢出治疗不计入 + 排位实测
11. 换宠 60s CD vs 普通局 15~40s → 定位为 BOSS/深渊局机制（有意设计，已注明）
12. 每日免费抽连 7 天奖与签到第 7 天大奖重复 → 免费抽连签改灵魂石×50
13. 突破材料消耗未量化 → 10/15/20/25/30 定稿，重复转化 20/40 与之对齐
14. 装备获取单渠道（仅抽奖）→ 补 BOSS 掉落+商店
15. 深渊 25 层 ×4 缩放 vs lv60 满养成 → 终层 TTK 模拟

**C 类：文案/口径小修（零成本窗口内已改）**
16. 霜语技能"炽焰"与冰霜主题违和 → 改名"霜刃"（fs_shuangren）；此时技能 icon 未制作，改名为零成本，素材开工后即变返工
17. v6 母图估算 65~75 与其明细表合计不符（漏计小图标）→ §8 口径说明，以素材总清单为准
