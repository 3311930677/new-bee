# 《远征》夜间自动迭代日志（2026-09-19 起，接 09-18 日志轮次 12 之后）

> 用户指令：夜间自动迭代直至时间结束；每轮 ①排优先级 → ②1 主 + ≤2 副问题 → ③至少 1 个新玩法/内容
> → ④验证（能启动/构建/过回归）→ ⑤更新日志与素材来源 → ⑥Git 提交或备份快照 → ⑦自动进入下一轮。
> 本夜用户点名四件事：**探索玩法无聊（选宝箱就直接跑过去、无位置提示、无地图、漫长且无聊）；
> 首页设置没做好；ESC 退出时灵时不灵；手游每个界面都要有能点的返回键。**
> 运行环境（与本机交接文档不同，以本机为准）：Godot **4.7.2** stable
> （`C:\Users\Administrator\AppData\Local\Microsoft\WinGet\Packages\GodotEngine...\Godot_v4.7.2-stable_win64.exe`），
> 项目根 `d:\new bee\远征`，PowerShell。回归命令：`powershell -NoProfile -File tools/run_regression.ps1`（22 项，
> 已加 `--quit-after` 护栏与 `-List` 分批参数；**脚本必须保持纯 ASCII**，PS5.1 按 ANSI 读脚本曾把中文路径读坏）。

---

## 环境整备（开工先修的两件硬事）

1. **Git 仓库离线不可用 → 已修复。**
   现象：`git status / add / commit / write-tree` 全部报
   `could not fetch <sha> from promisor remote`（仓库是 `partialclonefilter=blob:none` 的**部分克隆**，
   本机没有网络，历史上 blob 拉不下来；`.git/index` 也丢了）。
   修法：`git read-tree HEAD` 重建索引 → `git add -A`（把工作区全部文件**重新哈希入库**，缺失的 blob
   由本地内容等价重建）→ `git write-tree` / `git commit-tree` / `git update-ref refs/heads/main` 完成
   本地提交（绕开需要旧 blob 的 diff 统计）。**代价**：`add -A` 约 67s；旧历史 parent 仍缺（`git log` 只能
   到本机第一次克隆的深度），不影响开发。**push 仍需网络，恢复联网后 `git push` 即可。**
2. **回归脚本 `tools/run_regression.ps1`（新）**：22 项 headless 一键跑，支持 `-List a,b,c` 分批、
   `--quit-after` 防挂死（脚本编译错误时协程不会 quit，headless 会永远挂着）、失败自动摘错误行。
   注：`Start-Process -ArgumentList` 会按空格重新拆参，把 `d:\new bee\远征` 拆成 `d:\new`——**必须用 `&` 调用**。

## 轮次 13｜探索导航三件套：小地图 + 目标罗盘（自动前往）+ 疾行

**现状缺口**（用户点名）：
1. **主｜探索"盲走"**：地图 32×42 格（1536×2016px），相机 1.25 倍视野只有约 1/5 屏，没有小地图、
   没有位置提示——玩家不知道自己在哪、传送阵在哪、宝箱在哪。
2. **主｜空跑漫长**：出生点到传送阵纵向 1776px，88px/s 步速要 **20 秒起**，宝箱节点一来一回 25~35s，
   全程只有推摇杆，没有别的可做。
3. 副｜目标提示只有一行静态小字（"开启宝箱，然后前往传送阵"），没有方向、没有距离。

### 13.1 本轮改动（全在 `src/explore/MapScene.gd` + `data/maps.json`）

| # | 类别 | 改动 |
|---|---|---|
| 主 | 导航 | **小地图 `_Minimap`**：右上角 78×102（与地图同比例），金点=玩家（带朝向短须）、青点=传送阵（封印转紫）、金菱=宝箱/事件/商队/篝火、红点=敌影（**只有 520px 视野内才显形**，远处保留未知，不做成上帝视角）、亮框=当前视野范围；点击放大成 260×342 大地图（含图例说明 + 「前 往」/「返 回」按钮，ESC 也可关） |
| 主 | 导航 | **目标罗盘 `_Compass`**：目标名 + 实时步数（1 步=1 格）+ 旋转箭头；**点它=自动前往**（把"按 20 秒摇杆"变成一次点击）。导航目标优先级：未使用的物件 → 封印时的守阵首领 → 传送阵 |
| 主 | 玩法 | **自动前往**：直线奔目标 + 卡住检测（0.22s 位移不足就换侧绕行 0.5s）+ 超时 26s 自停（"前路不通——请你亲自来"）；**任何手动输入立刻接手**（键盘/摇杆都行），不跟玩家抢方向盘 |
| 主 | 玩法 | **疾行开关**：右下角「疾行 · 开/关」，速度 ×`sprint_mult`；步频动画同步提速（基准从写死 88 改读配置） |
| 副 | 数据 | `maps.json`：`player_speed 88→100`、新增 `sprint_mult:1.7`、`map_reveal_radius:520`（全表驱动，无魔法数） |
| 副 | 版式 | HUD 重排：目标罗盘插到目标小签下（y78），HP/药/换宠整行下移 26px，「撤离」让位小地图移到其正下方（404,126），疾行在右下（386,600） |
| 副 | 交互 | 大地图浮层纳入 ESC 层级（大地图 → 撤离确认 → 撤离）；`_physics_process` 在大地图打开时冻结移动 |
| 副 | 回归 | `VerifyMapScene` 扩 B3 + J 段共 14 条断言：小地图/罗盘尺寸与铺开、罗盘指对目标（普通区→传送阵、宝箱区→宝箱）、步数文案、疾行位移>步行×1.3、自动前往 340 帧内走到并**触发开箱**、再点罗盘停下、手动输入立刻取消（键盘 + 摇杆两条路径）、大地图开关与 ESC |

**素材**：本轮零新素材，小地图/罗盘全部程序绘制（`_draw` 圆点/菱形/箭头），符合"无素材也能跑"的口径。

### 13.2 验证

- **22 项 headless 回归全绿**（分 4 批跑，`VerifyMapScene` 扩至 J 段）。
- 截图复查 `map`：小地图右上（玩家金点 + 传送阵青点 + 视野框）、罗盘「▲ 传送阵 · 还有 37 步」、
  疾行钮、HP 行下移无遮挡——版式与预期一致。
- 过程修的坑：GDScript `match` 的默认分支写成了 `_ = Vector2(...)`（编译炸了全局类 MapScene，
  连带 VerifyMapScene 全链编译失败）→ 改 `_: face = ...`；两处 `:=` 从 Variant 推断（坑 4）→ 显式标注。

### 13.3 未解决 / 下轮计划

- 小地图不显示地形轮廓（只有点位），等后续做"走过才点亮"的迷雾探索时一并升级。
- 自动前往没有真正的寻路（A*），绕行是启发式的；地图无墙体、只有散件碰撞，实测够用。
- **轮次 14（已定）**：全界面返回键 + ESC 层级统一（用户点名：ESC 时灵时不灵、手游每个界面都要能点返回）。
  初步盘点缺 ESC 的浮层：WorldPanel / CodexPanel / DeployPanel / ExchangePanel / GachaPanel / SettingsPanel / TalentPanel。
- **轮次 15（预定）**：设置面板补全（用户点名"首页的设置还没做好"）。

---

## 轮次 14｜返回与 ESC 分层统一：手游每个界面都要有能点的返回键

**现状缺口**（用户点名"ESC 退出还是有时候会有问题，而且这是手游，每个界面都要有能点击返回的按键适配"）：
1. **主｜ESC 时灵时不灵**：全项目 20+ 个浮层各写各的 ESC，**半数浮层压根没处理**
   （WorldPanel / CodexPanel / DeployPanel / ExchangePanel / GachaPanel / SettingsPanel / TalentPanel），
   只能靠父层 GameHome / CityScene 兜底；而父层的兜底不认"谁在最上层"。
2. **主｜ESC 层级错乱**：已经有 `_unhandled_input` 的浮层里，**十余个没有 `G.ui_blocked` 守卫**——
   开着 GM 控制台按 ESC，会把控制台背后的浮层一起关掉（"时灵时不灵"的根因就在这）。
3. 副｜三选一词条浮层不吃 ESC：PC 玩家按 ESC 完全没反应（界面上明明有「放弃」这个选择）。
4. 副｜登录页没有可见返回键（只有 ESC），手游从标题页进来就出不去了。
5. 副｜**幽灵按钮在暗底上"字没了"**：`ghost_button` 的字是深棕 `6a4a1e`，压在召唤结果层这种
   整屏暗底（`VEIL_TAKEOVER_A` 0.94）上肉眼几乎看不见——"有按钮但看不见"等于没有返回键。

### 14.1 本轮改动

| # | 类别 | 改动 | 文件 |
|---|---|---|---|
| 主 | 返回 | **7 个缺 ESC 的浮层补齐**：自己吃 `ui_cancel` 并 `set_input_as_handled()`（不再靠父层兜底） | WorldPanel / CodexPanel / DeployPanel(Esc=取消筹备) / ExchangePanel / GachaPanel / SettingsPanel / TalentPanel |
| 主 | 层级 | **十余个浮层补 `G.ui_blocked` 守卫**：GM 控制台等全屏层打开时，ESC 只给最上层 | ArenaPanel / TitlePanel / SkillBookPanel / QuestPanel / PetRaisePanel / MountPanel / EquipPanel / AvatarPanel / GrowthPanel / StoryBeat / Prologue / Title / Login / CreateRole / RouteScene |
| 副 1 | 交互 | **TraitPicker（词条三选一）吃 ESC = 放弃**：走界面上本来就有的「放弃」分支，不新增规则 | TraitPicker.gd |
| 副 2 | 手游适配 | **登录页补可见「返回」（回标题）**：原来只有 ESC，手机上没有回头路 | Login.gd |
| 副 3 | 可见性 | `G.ghost_button()` 新增 `text_color` 参数：传浅色字时自动切"暗底 + 金描边"；**召唤结果层「再抽一次 / 返回」这排按钮不再隐形** | G.gd、GachaPanel.gd |
| 副 4 | 回归 | 新增 `VerifyNav`（23 项 → 22+1）：①每个浮层实例化后必须找得到可见返回类控件（返回/关闭/跳过/取消/撤退/退出）；②每个浮层脚本必须自己吃 ESC；③吃 ESC 必须带 `ui_blocked` 守卫（源码扫描，`Audio.sfx("ui_cancel")` 这类字符串不算）；④运行期验证 ui_blocked 时 ESC 关不掉、复位后能关、点按钮也能关 | tools/VerifyNav.gd/.tscn（新） |

### 14.2 验证

- **23 项 headless 全绿**（新增 `VerifyNav`，回归清单 17 场景 + 5 脚本 + 1 新场景）。
- `VerifyNav` 首跑**真的抓到 4 条**：LoadScreen 被判缺 ESC（过渡页，加白名单并写明理由）、
  TraitPicker 的 `Audio.sfx("ui_cancel")` 字符串被误判（判定收紧为 `is_action_pressed("ui_cancel")`）、
  以及两处"计数为 0"——**又是坑 3：lambda 值是拷贝**，改用字典容器后才正常。
- 整包编辑期导入 `--editor --quit`：**0 ERROR**。
- 截图复查 `login`（返回键首版用幽灵钮，**深棕字压在晚霞插画上几乎看不见**→ 改金钮后重截确认）、
  `settings` / `map`——返回键都在位。

### 14.3 未解决 / 风险

- ESC 仍是"每个浮层自己处理"的分散式约定，靠 `VerifyNav` 守；没有做全局 ESC 栈（浮层注册制）。
  当前分层正确（深节点先吃、吃到就 handled），够用；若以后出现"浮层叠浮层"再考虑注册制。
- 设置面板本身仍未补全（用户点名的"设置没做好"），排轮次 15。

---

## 轮次 15｜设置面板重做：从"一页塞满开发者功能"到两页玩家设置

**现状缺口**（用户点名"首页的设置目前还没做好"）：
1. **主｜主次颠倒 + 拥挤**：440×524 一页塞了 12 个控件，字号被压到 13px；
   「导出/导入存档码」这种**开发者向功能占据上半屏**，真正的玩家设置（声音）挤在最下面还有
   一句"（放 assets/audio/ 即生效）"的开发注释常驻。
2. **主｜只有声音能调，别的都不能调**：一个手游的设置页只有音量滑条，没有震屏 / 演出 / 倍速 /
   昵称 / 版本信息，玩家想改手感无从下手（而战斗侧其实早就有这些开关的"原料"）。
3. 副｜音量、静音这排设置没地方放说明，改完也不知道写了没写（无反馈）。
4. 副｜设置项没有回归约束：以后谁把某个开关改成"只在内存里改、不写存档"，不会有测试发现。

### 15.1 本轮改动

| # | 类别 | 改动 | 文件 |
|---|---|---|---|
| 主 | 版式 | **两页重做**（复用 `PageDeck`，拖拽/箭头/圆点/←→ 全支持）：<br>**第 1 页 常规**：声音（音乐/音效/静音）→ 战斗与演出（震屏 / 剧情演出 / 战斗倍速）→ 玩家（昵称 + 更换头像）<br>**第 2 页 存档与系统**：存档码备份/导入 → 剧情与角色（重看序章/重选角色/回标题）→ 危险操作（重置）→ 版本行 | SettingsPanel.gd |
| 主 | 设置项 | **新增 4 项真设置**，全部存 `prog.settings` 跟着存档走：<br>`shake`（受击震屏，默认开）→ `BattleScene._shake()` 直接早退<br>`skip_story`（剧情演出，默认播）→ 首领对峙/战后余韵直接跳过（**不标记已看**，回头开了还能补看）<br>`battle_speed`（每场战斗开局默认倍速）→ 进战斗即生效<br>昵称（G.player_name，最多 8 字，空值拒绝） | G.gd / BattleScene.gd / MapScene.gd |
| 副 1 | API | `G.setting_get(key, def)` / `G.setting_set(key, value)`；读档补 `prog.settings`（老档缺省 `{}`，向后兼容） | G.gd |
| 副 2 | 交互 | 设置里的「更换头像」发 `avatar_requested` 信号，由主界面**先收设置、再叠头像层**（两个面板不叠在一起） | SettingsPanel.gd / GameHome.gd |
| 副 3 | 回归 | `VerifyPanels` 扩 3.5/3.6 段：两页存在、四个开关点击后**写进存档并能回读**、空昵称被拒、正常昵称落到 `G.player_name` | tools/VerifyPanels.gd |
| 副 4 | 工具 | `ShotRunner` 新增 `settings2` 用例（翻到第 2 页截图） | tools/ShotRunner.gd |

**素材**：本轮零新素材（全部复用既有 `ui_notice` / Kenney 箭头 / 程序绘制）。

### 15.2 验证

- **23 项 headless 全绿**（批次实跑：13 项大批 + 4 项战斗/面板 + 6 项其余）。
- 整包编辑期导入 `--editor --quit`：**0 ERROR**。
- 截图复查 `settings` / `settings2` 两页。**本轮踩到两个"肉眼才看得见"的坑**：
  ① 我把徽记的 `stretch_mode` 从 `KEEP_ASPECT_CENTERED` 改成 `KEEP_CENTERED`，
  44×320 的 `ui_notice` 于是**按原尺寸铺满整个上半屏**（448×320 的华丽卷轴糊住了设置页）→ 改回；
  ② 手摆坐标的说明 Label **不会被父级约束宽度**，文本最小宽把它顶出面板右缘 → 显式给 `size` 并断行。
- 过程修的**真 bug（回归抓出来的）**：`BattleScene.speed` 我原本在 `_build_hud` 里赋默认值，
  而回归用例是 `instantiate → speed=12 → add_child`，默认值把用例设的 12 倍速吃掉了，
  于是"12 倍速跑完一场"变成"1 倍速等到超时"（`普通节点应胜利` 失败）。改成
  **负哨兵 `speed = -1` + `cur_speed()`**：没人指定时才用设置默认档，外部显式指定一律尊重。

### 15.3 未解决 / 风险

- 设置页没有"恢复默认设置"按钮（四个开关手动点回去也不麻烦，先不加）。
- `skip_story` 只作用于**首领对峙/余韵**；序章（一次性）与词条三选一不受它影响。
- 昵称只改显示名（`G.player_name`），角色名（roles.json 里的职业名）不动——两者在主页顶部
  同时出现时以昵称为准。

---

## 轮次 16｜探索动机：让"清一遍"不再是浪费时间

**现状缺口**：
1. **主｜绕怪直冲永远是最优解**：轮次 13 解决了"看不见路"，但地图上除了"走到传送阵"没有第二个目标。
   怪物可以绕、篝火可以不吃，一路直冲传送阵反而收益最高——探索层沦为过道，玩几分钟就腻。
2. 副｜**路上没有微反馈**：从出生点到传送阵那段路（20 秒起）走过一次就再没有新东西，
   全程只有"赶路"一个动作。
3. 副｜**清场的收益不可见**：打赢每一场只涨数字，玩家不知道"多打几只"值不值。

### 16.1 本轮改动

| # | 类别 | 改动 | 文件 |
|---|---|---|---|
| 主 | 玩法 | **探索分与评价**：击杀 +12 / 拾取 +6 / 全图清剿 +30，按阈值分三档评价「匆匆过客 → 踏遍此地 → 寸土必争」，**通关时结算附加赏金**（40 / 90 / 160） | MapScene.gd |
| 主 | 玩法 | **清剿赏**：把全图怪物清空（BOSS 区也能做）立刻给 `rewards.clear`（金币 260 / 远征币 40 / 经验 110 / 荣誉 2）并 toast 报数——"清场"终于有落点，且只给一次 | MapScene.gd、nodes.json |
| 副 1 | 玩法 | **散落拾取物**：每张图 3~5 个钱袋/魂晶（金色钱袋 / 淡青魂晶，带落地光斑与上下浮动），走近 30px 自动入袋（金币 18~42 + 远征币 3~8 + 探索分） | MapScene.gd（`_Pickup`） |
| 副 2 | 可读性 | HUD 新增**探索小签**（HP 条上方）：「清剿 x/y · 评价」；无怪节点改显「探索分 n · 评价」；评价提升时有一句提示 | MapScene.gd |
| 副 3 | 可读性 | 小地图把未拾取的拾取物画成亮点（与地面光斑同色），大地图图例同步补两条 | MapScene.gd |
| 副 4 | 数据 | `nodes.json` 新增 `rewards.clear` 与 `explore` 段（拾取数量/金额区间/分值/评价阈值/附加赏/拾取半径），**全部表驱动，代码里无魔法数** | data/nodes.json |
| 副 5 | 回归 | `VerifyMapScene` 扩 K 段 15 条断言：拾取物数量区间 / 走近自动入袋并给双币 / 加分 / HUD 文案 / 评价分档（低分无赏、满分 +160）/ 通关结算附加赏 / 清剿赏只给一次 / 击杀计数与加分 | tools/VerifyMapScene.gd |

**素材**：本轮零新素材（拾取物、光斑、HUD 全部程序绘制；无外部素材引入，故 `素材来源与许可证.md` 无需新增条目）。

### 16.2 验证

- **23 项 headless 全绿**（分两批：9 项探索/战斗/面板 + 14 项其余）。
- 截图复查 `map`：探索小签「清剿 0/3 · 匆匆过客」、小地图上多了金/青亮点、HUD 五行不重叠。
- 顺带确认：路线结算的收入口径没被探索赏串味（`VerifyRouteScene` 仍绿）。

### 16.3 未解决 / 风险

- 探索分不做跨节点累计（每个节点独立评价）——有意为之：避免"攒一局分"变成压力。
- 拾取物均匀撒点，不做"藏在角落"的稀有拾取（那需要真正的兴趣点系统，留待后续）。
- 清剿赏只给货币，**没有给材料**——材料仍走 drops.json，避免两条产出线互相稀释。

---

## 轮次 17｜兴趣点与音效：给"路上遇到的东西"加一层取舍

**现状缺口**：
1. **主｜探索只有被动奖励**：轮次 16 的拾取物是"走过去自动入袋"，玩家没有任何决定；
   地图上缺"要不要花代价换收益"的选择点。
2. 副｜**拾取音复用战斗后的 `coin`**：一局要捡 3~5 次，同一个"成交感"的重音重复出现，听感发木。
3. 副｜**音频资产只有 18 件**，探索层的新反馈（拾取/祭坛）没有对应音色。

### 17.1 本轮改动

| # | 类别 | 改动 | 文件 |
|---|---|---|---|
| 主 | 玩法 | **碑灵祭坛**（新兴趣点，每图约 60% 概率 1 座）：走近弹出选择框——「献金 200 · 重摇一次祝福」或「离开」。献金真扣局内金币（会被远征结算算进去，是真代价），成功则复用 `TraitPicker` 重择词条；金币不足→拒绝并提示；词条池已尽→**退钱**并说明。用掉即熄，不重复打扰 | MapScene.gd、nodes.json |
| 主 | 玩法 | **矿脉**（每图 1~2 处）：走近白拿养成材料（强化石/精炼石/宠物粮 ×1~2）并计入探索分——"刷图出材料"多一条稳定的路 | MapScene.gd、nodes.json |
| 副 1 | 音效 | `tools/make_sfx.py` 新增两件**程序合成**音效：`pickup`（木片轻叩 + 高音小铃，刻意比 `coin` 小一号）与 `altar`（低沉钟鸣 + 张开的低通气声，"碑里有人应了一声"）；清单 18 → 20 件 | tools/make_sfx.py、assets/audio/pickup.wav·altar.wav |
| 副 2 | 接线 | `Audio.SFX_NAMES` 登记两件新音效（加载页预热与 `VerifyAudio` 都按这份清单走）；`on_pickup` 改播 `pickup`，祭坛献金播 `altar` | Audio.gd、MapScene.gd |
| 副 3 | 可读性 | 小地图新增兴趣点图例：紫菱=祭坛 · 灰点=矿脉；大地图图例补两条说明 | MapScene.gd |
| 副 4 | 回归 | `VerifyAudio` 清单下限 17 → 20；`VerifyMapScene` 扩 L 段 11 条：兴趣点数量、矿脉给材料并加探索分、矿脉用掉即移除、祭坛弹层/扣费/关闭/重摇、金不足拒绝且不扣费、离开关层 | tools/VerifyAudio.gd、tools/VerifyMapScene.gd |

**素材**：本轮**零外部素材**。两件新音效是 `tools/make_sfx.py` 程序合成（自产，无第三方版权）。
关于 BGM 替换：夜间实测 **FreePD 已永久关闭**、OpenGameArt 需逐曲核对许可、Pixabay 对本机 403，
故**未下载任何文件**，改为在 `素材来源与许可证.md` §5.1 写清「搜索关键词 + 情绪/时长条件 + 候选来源 + 核验口径」，标记**需人工下载**。

### 17.2 验证

- **23 项 headless 全绿**（10 项 + 13 项两批）。
- 截图复查 `map`；`godot --headless --import` 后新音效已进资源库（`VerifyAudio` 逐个 `sfx_path()` 断言不缺件）。
- 过程踩的坑：`_Minimap._draw()` 里已有 `var s :=` 地图缩放比，我新写的 `for s in map_ref._spots` **同名撞车直接编译失败**
  ——错误信息只报行号不给变量名对照，用 `--check-only --script` 才定位到（已改名为 `spot`，并在注释里写明）。

### 17.3 未解决 / 风险

- 祭坛只做"重摇词条"一种用途；将来可扩成"洗装备词条/回满药水"等（数据驱动即可加）。
- 矿脉材料直接进背包，不弹明细层（走 toast）——够用，但缺少"这是什么材料"的解释，留待材料图鉴。

---

## 夜间收官准备（若时间够，下一轮候选）
